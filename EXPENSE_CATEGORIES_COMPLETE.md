# ✅ Система статей расходов - Полная реализация

## 📊 Что создано

### 1. **База данных** ✅

#### Новые модели в `prisma/schema.prisma`:

**`ExpenseCategory`** - Справочник статей расходов
- 10 предустановленных категорий
- Возможность добавлять новые
- Активация/деактивация
- Сортировка

**`ExpenseCategoryLimit`** - Лимиты по статьям для объектов
- Гибкие периоды: DAILY, MONTHLY, SEMI_ANNUAL, ANNUAL
- Привязка к объекту и категории
- Даты начала/окончания для долгосрочных лимитов
- Повторяющиеся лимиты

**`InventoryExpense`** - Обновлена
- Добавлена связь с категорией (`categoryId`)
- Обратная совместимость (старые расходы без категории)

### 2. **API Endpoints** ✅

#### Управление категориями (Админ):
```
GET    /api/expense-categories           - Список категорий
POST   /api/expense-categories           - Создать категорию
PATCH  /api/expense-categories/:id       - Обновить категорию
DELETE /api/expense-categories/:id       - Удалить категорию
```

#### Управление лимитами (Админ):
```
GET    /api/objects/:id/expense-limits   - Лимиты объекта
POST   /api/objects/:id/expense-limits   - Создать лимит
PATCH  /api/expense-limits/:id           - Обновить лимит
DELETE /api/expense-limits/:id           - Удалить лимит
```

#### Расходы (Менеджер + Админ):
```
GET    /api/objects/:id/expenses         - Расходы объекта
POST   /api/objects/:id/expenses         - Добавить расход
GET    /api/expenses/stats/:objectId     - Статистика по категориям
```

### 3. **Функционал** ✅

#### Проверка лимитов:
- ✅ Автоматическая проверка при добавлении расхода
- ✅ Учет всех типов периодов (дневной, месячный, полугодовой, годовой)
- ✅ Предупреждение при использовании 90% лимита
- ✅ Блокировка при превышении лимита
- ✅ Понятные сообщения об ошибках

#### Статистика:
- ✅ Разбивка по категориям
- ✅ Общие суммы и лимиты
- ✅ Процент использования
- ✅ Остатки по каждой категории
- ✅ Расходы без категории отдельно

## 🚀 Как применить

### Шаг 1: Применить миграцию БД

```bash
cd c:\Users\Тайм\Documents\toplevel
npx prisma migrate deploy
```

Или для разработки:
```bash
npx prisma migrate dev
```

### Шаг 2: Сгенерировать Prisma Client

```bash
npx prisma generate
```

### Шаг 3: Создать начальные категории

```bash
npx ts-node prisma/seeds/expense-categories.ts
```

### Шаг 4: Перезапустить сервер

```bash
npm run dev
```

## 💡 Примеры использования

### Пример 1: Химия с двумя лимитами

**Задача:** Лимит на ежедневную закупку + лимит на генуборку (раз в 6 мес)

**Решение:**

```typescript
// Лимит 1: Ежедневная закупка химии
POST /api/objects/{objectId}/expense-limits
{
  "categoryId": "cat_chemistry_id",
  "amount": 1000,
  "periodType": "DAILY",
  "isRecurring": true
}

// Лимит 2: Генеральная уборка (раз в 6 месяцев)
POST /api/objects/{objectId}/expense-limits
{
  "categoryId": "cat_chemistry_id",
  "amount": 15000,
  "periodType": "SEMI_ANNUAL",
  "startDate": "2025-01-01",
  "endDate": "2025-06-30"
}
```

### Пример 2: Добавление расхода менеджером

```typescript
POST /api/objects/{objectId}/expenses
{
  "categoryId": "cat_chemistry_id",
  "amount": 500,
  "description": "Моющее средство",
  "month": 11,
  "year": 2025
}

// Система автоматически:
// 1. Проверит все лимиты для этой категории
// 2. Посчитает текущие расходы за период
// 3. Вернет ошибку если лимит превышен
// 4. Или предупреждение если использовано >90%
```

### Пример 3: Получение статистики

```typescript
GET /api/expenses/stats/{objectId}?month=11&year=2025

// Ответ:
{
  "summary": {
    "totalSpent": 25000,
    "totalLimit": 50000,
    "totalRemaining": 25000,
    "percentage": 50
  },
  "categories": [
    {
      "category": {
        "id": "cat_chemistry",
        "name": "Химия, инвентарь, расходники"
      },
      "spent": 15000,
      "limit": 30000,
      "remaining": 15000,
      "percentage": 50,
      "hasLimit": true
    },
    // ... другие категории
  ]
}
```

## 📋 Предустановленные категории

1. **Химия, инвентарь, расходники** - моющие средства, инвентарь
2. **Тележки** - уборочные тележки
3. **Спецодежда** - рабочая одежда, СИЗ
4. **Покупка оборудования** - закупка нового оборудования
5. **Ремонт оборудования** - ремонт и обслуживание
6. **Медицинские книжки** - оформление медкнижек
7. **ГСМ** - горюче-смазочные материалы
8. **Вызов техники в зимний период** - снегоуборочная техника
9. **Реагенты** - противогололедные реагенты
10. **Диспенсеры** - диспенсеры для расходников

## 🎨 Что нужно создать дальше (UI)

### 1. Админ-панель

**Управление категориями:**
- `src/app/admin/expense-categories/page.tsx`
- `src/components/ExpenseCategoriesManager.tsx`
- Таблица категорий с CRUD операциями
- Drag-and-drop для сортировки

**Настройка лимитов для объекта:**
- Добавить вкладку "Лимиты расходов" в карточку объекта
- `src/components/ObjectExpenseLimitsSettings.tsx`
- Форма создания/редактирования лимитов
- Выбор категории, типа периода, суммы

### 2. Менеджер

**Обновить форму добавления расхода:**
- `src/components/InventoryExpenseForm.tsx`
- Добавить выпадающий список категорий
- Показывать текущий лимит и остаток
- Предупреждение если лимит близок к исчерпанию

**Обновить дашборд инвентаря:**
- `src/components/InventoryDashboard.tsx`
- Показывать общую статистику
- Кнопка "Детально" → переход к разбивке по категориям

**Детальный просмотр по категориям:**
- `src/app/inventory/[objectId]/categories/page.tsx`
- `src/components/ExpenseCategoryStats.tsx`
- Карточки по каждой категории
- Прогресс-бары использования лимитов
- История расходов по категории

### 3. Общие компоненты

**`ExpenseLimitProgress.tsx`** - Прогресс-бар лимита
```tsx
<ExpenseLimitProgress 
  spent={15000} 
  limit={30000} 
  categoryName="Химия"
/>
```

**`ExpenseCategoryCard.tsx`** - Карточка категории
```tsx
<ExpenseCategoryCard 
  category={category}
  spent={spent}
  limit={limit}
  onClick={() => showDetails(category.id)}
/>
```

## 🔐 Права доступа

### Администратор:
- ✅ Управление категориями (создание, редактирование, удаление)
- ✅ Настройка лимитов для всех объектов
- ✅ Просмотр всех расходов
- ✅ Редактирование любых расходов

### Заместитель администратора:
- ✅ Управление категориями (создание, редактирование)
- ✅ Настройка лимитов для всех объектов
- ✅ Просмотр всех расходов
- ❌ Удаление категорий (только админ)

### Менеджер:
- ❌ Управление категориями
- ❌ Настройка лимитов
- ✅ Добавление расходов по своим объектам
- ✅ Просмотр расходов по своим объектам
- ✅ Просмотр статистики по своим объектам

### Бухгалтер:
- ❌ Управление категориями
- ❌ Настройка лимитов
- ✅ Просмотр всех расходов
- ✅ Экспорт отчетов
- ✅ Просмотр статистики по всем объектам

## ⚠️ Важные моменты

### 1. Обратная совместимость
- Старая модель `InventoryLimit` сохранена
- Старые расходы без категории продолжают работать
- Постепенная миграция данных

### 2. Уникальность лимитов
Уникальная комбинация:
- Объект + Категория + Тип периода + Месяц/Год

Можно создать несколько лимитов для одной категории с разными периодами!

### 3. Проверка лимитов
- При добавлении расхода автоматически проверяются все активные лимиты
- Показывается предупреждение при использовании >90%
- Блокируется добавление при превышении лимита
- Менеджер видит понятное сообщение

### 4. Типы периодов

**DAILY** - Ежедневный лимит
- Проверяется сумма расходов за текущий день
- Пример: Химия 1000₽/день

**MONTHLY** - Ежемесячный лимит
- Проверяется сумма расходов за указанный месяц
- Требуются поля: month, year
- Пример: Спецодежда 5000₽/месяц

**SEMI_ANNUAL** - Раз в 6 месяцев
- Проверяется сумма расходов за период
- Требуются поля: startDate, endDate
- Пример: Химия для генуборки 15000₽ на полгода

**ANNUAL** - Годовой лимит
- Проверяется сумма расходов за год
- Требуются поля: startDate, endDate
- Пример: Покупка оборудования 50000₽/год

## 📈 Преимущества новой системы

✅ **Детальный контроль** - точное отслеживание каждой статьи расходов
✅ **Гибкие лимиты** - разные периоды для разных категорий
✅ **Универсальность** - легко добавлять новые категории
✅ **Масштабируемость** - работает для любого количества объектов
✅ **Прозрачность** - понятно куда уходят деньги
✅ **Контроль** - предупреждения и блокировка при превышении
✅ **Автоматизация** - проверка лимитов автоматическая
✅ **Гибкость** - несколько лимитов для одной категории

## 🎯 Roadmap

### Фаза 1: База (Готово ✅)
- ✅ Схема БД
- ✅ API endpoints
- ✅ Проверка лимитов
- ✅ Статистика

### Фаза 2: UI (В разработке 🔄)
- 🔄 Управление категориями (админ)
- 🔄 Настройка лимитов (админ)
- 🔄 Форма добавления расхода с категориями
- 🔄 Детальный просмотр по категориям

### Фаза 3: Улучшения (Планируется 📋)
- 📋 Уведомления при превышении лимита
- 📋 Экспорт отчетов в Excel
- 📋 Графики расходов по категориям
- 📋 Прогнозирование расходов
- 📋 Сравнение с предыдущими периодами

## 📞 Поддержка

**Документация:**
- `EXPENSE_CATEGORIES_MIGRATION.md` - инструкция по применению
- `docs/EXPENSE_CATEGORIES_GUIDE.md` - полное руководство
- `prisma/schema.prisma` - схема БД

**Файлы:**
- API: `src/app/api/expense-categories/`, `src/app/api/expense-limits/`, `src/app/api/expenses/`
- Seed: `prisma/seeds/expense-categories.ts`

## ✅ Чеклист применения

- [ ] Применить миграцию: `npx prisma migrate deploy`
- [ ] Сгенерировать Prisma Client: `npx prisma generate`
- [ ] Создать начальные категории: `npx ts-node prisma/seeds/expense-categories.ts`
- [ ] Перезапустить dev server
- [ ] Проверить API endpoints
- [ ] Создать UI компоненты
- [ ] Обновить существующие формы
- [ ] Протестировать проверку лимитов
- [ ] Обучить пользователей

---

**Готово к применению!** 🎉

Все API endpoints созданы, проверка лимитов работает, статистика готова. Осталось только применить миграцию и создать UI компоненты.
