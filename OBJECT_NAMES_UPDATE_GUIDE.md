# 📋 Руководство по обновлению названий объектов

## 🎯 Цель
Привести названия объектов к единому виду, исправить регистр, убрать лишние символы и цифры, при этом сохранив все связи с менеджерами и функционал системы.

## ⚠️ ВАЖНО: Какие названия брать за эталон?

### ✅ БЕРИТЕ НАЗВАНИЯ ИЗ ТАБЛИЦЫ `CleaningObject`

**Почему:**
1. **CleaningObject - источник истины** - основная таблица объектов с уникальными ID
2. **Все связи используют ID** - техкарты, комнаты, участки ссылаются на `CleaningObject.id`
3. **User.name - это имена людей** - менеджеры могут меняться, объекты остаются
4. **Минимум изменений** - весь код уже работает с `CleaningObject.name`

### ❌ НЕ БЕРИТЕ названия из User (менеджеры)
- Менеджеры - это люди, не объекты
- Один менеджер может управлять несколькими объектами
- Названия объектов не должны зависеть от имен менеджеров

---

## 📝 Пошаговая инструкция

### Шаг 1: Экспорт текущих названий

Запустите команду для экспорта всех текущих названий объектов:

```bash
npm run export-object-names
```

**Результат:** Создастся файл `scripts/current-object-names.json` со структурой:

```json
[
  {
    "objectId": "clxxx...",
    "oldName": "Магазин №1",
    "newName": "Магазин №1",
    "managerName": "Иванов Иван",
    "techCardsCount": 15,
    "roomsCount": 5,
    "checklistsCount": 120
  }
]
```

### Шаг 2: Редактирование в Excel

1. Откройте `current-object-names.json` в Excel или текстовом редакторе
2. Отредактируйте поле `newName` для каждого объекта:
   - Приведите к единому регистру (например, "Магазин №1" вместо "МАГАЗИН №1")
   - Уберите лишние пробелы
   - Уберите лишние цифры и символы
   - Унифицируйте названия (например, все "Магазин" или все "Торговый центр")

**Пример исправлений:**
```
❌ "МАГАЗИН  №1"     → ✅ "Магазин №1"
❌ "магазин 1"       → ✅ "Магазин №1"
❌ "Магазин#1"       → ✅ "Магазин №1"
❌ "ТЦ Европа 123"   → ✅ "ТЦ Европа"
```

3. Сохраните файл как `scripts/object-names.json`

### Шаг 3: Проверка перед обновлением

**КРИТИЧЕСКИ ВАЖНО:**
- Убедитесь, что `objectId` не изменился
- Проверьте, что `newName` не пустое
- Проверьте, что нет дубликатов в `newName`

### Шаг 4: Запуск обновления

```bash
npm run update-object-names
```

**Что произойдет:**
1. Обновятся названия в таблице `CleaningObject`
2. Обновятся названия в таблице `Task` (поле `objectName`)
3. Все связи по ID останутся нетронутыми
4. Выведется статистика обновлений

**Пример вывода:**
```
🔍 Обрабатываем объект: МАГАЗИН №1
   ID: clxxx...
   Новое название: Магазин №1
   📋 Связанные данные:
      - Техкарт: 15
      - Помещений: 5
      - Чеклистов: 120
   ✅ Название объекта обновлено
   ✅ Обновлено задач: 45

============================================================
📊 ИТОГОВАЯ СТАТИСТИКА:
============================================================
✅ Обновлено объектов: 25/25
✅ Обновлено задач: 1250
```

---

## 🔒 Что сохранится после обновления

### ✅ Сохранятся все связи:
- Связь объекта с менеджером (`managerId`)
- Техкарты объекта (`TechCard.objectId`)
- Помещения объекта (`Room.objectId`)
- Участки объекта (`Site.objectId`)
- Чеклисты объекта (`Checklist.objectId`)

### ✅ Сохранится весь функционал:
- Календарь задач
- Вкладка "Менеджеры"
- Аналитика
- Дашборды
- Отчеты

### ⚠️ Что изменится:
- Названия объектов в интерфейсе
- Группировка задач (если были дубликаты, они объединятся)

---

## 🚨 Проблемы и решения

### Проблема: Дубликаты названий после обновления

**Причина:** Несколько объектов с разными ID получили одинаковое название

**Решение:**
1. Добавьте уникальный суффикс к названиям (например, "Магазин №1 (Центр)", "Магазин №1 (Север)")
2. Или объедините объекты (требует ручной работы с БД)

### Проблема: Задачи не обновились

**Причина:** В `Task.objectName` было другое название, не совпадающее с `oldName`

**Решение:**
1. Проверьте реальные названия в таблице `Task`:
   ```sql
   SELECT DISTINCT objectName FROM Task;
   ```
2. Добавьте все варианты в `oldName` или обновите вручную

---

## 📊 Рекомендации по унификации

### 1. Единый формат номеров
```
✅ "Магазин №1"
✅ "Магазин №2"
❌ "Магазин 1"
❌ "Магазин #1"
```

### 2. Единый регистр
```
✅ "Торговый центр Европа"
❌ "ТОРГОВЫЙ ЦЕНТР ЕВРОПА"
❌ "торговый центр европа"
```

### 3. Без лишних пробелов
```
✅ "Офис А"
❌ "Офис  А"
❌ " Офис А "
```

### 4. Единые сокращения
```
✅ "ТЦ Европа"
✅ "БЦ Москва"
❌ "Торговый центр Европа"
❌ "Бизнес-центр Москва"
```

---

## 🔄 Откат изменений

Если что-то пошло не так, вы можете откатить изменения:

1. Создайте резервную копию БД перед обновлением:
   ```bash
   pg_dump your_database > backup_before_update.sql
   ```

2. Или создайте обратный маппинг и запустите скрипт снова с `oldName` и `newName` поменянными местами

---

## ✅ Чеклист перед запуском

- [ ] Создана резервная копия БД
- [ ] Экспортированы текущие названия (`npm run export-object-names`)
- [ ] Отредактирован файл `object-names.json`
- [ ] Проверены все `objectId`
- [ ] Проверены все `newName` на дубликаты
- [ ] Нет пустых значений в `newName`
- [ ] Готовы к запуску обновления

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи выполнения скрипта
2. Проверьте структуру JSON файла
3. Убедитесь, что все `objectId` существуют в БД
