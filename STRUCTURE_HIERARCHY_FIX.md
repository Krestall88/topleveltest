# 🔧 Исправление отображения иерархии структуры

## Проблема
После создания зон и групп помещений они не отображались в модальном окне редактирования объекта.

## Причина
API endpoint `/api/objects/[id]` не включал вложенные зоны и группы помещений в ответ.

## Решение

### Обновлен файл: `src/app/api/objects/[id]/route.ts`

**Было:**
```typescript
sites: {
  include: {
    manager: {
      select: { id: true, name: true, email: true }
    }
  },
  orderBy: { name: 'asc' }
}
```

**Стало:**
```typescript
sites: {
  include: {
    manager: {
      select: { id: true, name: true, email: true }
    },
    zones: {
      include: {
        roomGroups: {
          include: {
            rooms: true
          }
        }
      }
    }
  },
  orderBy: { name: 'asc' }
}
```

## Результат

Теперь API возвращает полную иерархию:
```
CleaningObject
  └── sites[]
        ├── manager
        └── zones[]
              └── roomGroups[]
                    └── rooms[]
```

## Что теперь работает

✅ Создание участков - отображаются в модальном окне
✅ Создание зон - отображаются под участками
✅ Создание групп помещений - отображаются под зонами
✅ Создание помещений - отображаются под группами
✅ Редактирование всех элементов
✅ Удаление всех элементов

## Проверка

1. Откройте объект для редактирования
2. Перейдите на вкладку "Структура"
3. Добавьте участок
4. Добавьте зону к участку
5. Добавьте группу помещений к зоне
6. Добавьте помещение к группе

Все элементы должны отображаться в иерархическом виде с правильными отступами и иконками.
