# 📦 НАСТРОЙКА SUPABASE STORAGE ДЛЯ ЗАГРУЗКИ ФОТО

## 🎯 ЗАЧЕМ ЭТО НУЖНО

В вашем проекте есть функционал загрузки фотографий:
- Фотоотчеты по задачам
- Фото при завершении чеклистов
- Вложения к задачам отчетности

Для презентационной копии используем **Supabase Storage** вместо Timeweb S3.

---

## ⚡ БЫСТРАЯ НАСТРОЙКА (5 МИНУТ)

### 1️⃣ СОЗДАЙТЕ BUCKET В SUPABASE

1. **Откройте ваш проект в Supabase:**
   - Перейдите на [app.supabase.com](https://app.supabase.com)
   - Откройте проект `cleaning_control` (или как вы его назвали)

2. **Перейдите в Storage:**
   - В левом меню найдите иконку 🗄️ **Storage**
   - Нажмите на **Storage**

3. **Создайте новый bucket:**
   - Нажмите кнопку **"New bucket"** (справа вверху)
   - Заполните форму:
     ```
     Name: uploads
     Public bucket: ✅ (включите галочку)
     ```
   - Нажмите **"Create bucket"**

4. **Настройте политики доступа:**
   - Нажмите на созданный bucket `uploads`
   - Перейдите на вкладку **"Policies"**
   - Нажмите **"New Policy"**
   - Выберите шаблон **"Enable read access for all users"**
   - Нажмите **"Review"** → **"Save policy"**

### 2️⃣ ОБНОВИТЕ .ENV.LOCAL

Откройте файл `.env.local` в презентационной копии и раскомментируйте строку:

```env
# Было:
# NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET="uploads"

# Стало:
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET="uploads"
```

### 3️⃣ ГОТОВО! ✅

Теперь все фото будут загружаться в Supabase Storage.

---

## 🔄 ПЕРЕКЛЮЧЕНИЕ МЕЖДУ ХРАНИЛИЩАМИ

### Для презентации (Supabase Storage):
```env
# В .env.local презентационной копии
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET="uploads"

# S3 настройки закомментированы
# S3_ACCESS_KEY_ID="..."
# S3_SECRET_ACCESS_KEY="..."
```

### Для российских клиентов (Timeweb S3):
```env
# В .env.local клиента
S3_ACCESS_KEY_ID="ключ_клиента"
S3_SECRET_ACCESS_KEY="секрет_клиента"
S3_BUCKET_NAME="bucket_клиента"
S3_REGION="ru-1"
S3_ENDPOINT="https://s3.twcstorage.ru"

# Supabase Storage закомментирован
# NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET="uploads"
```

---

## 💡 ПРЕИМУЩЕСТВА ТАКОГО ПОДХОДА

### Для презентации (Supabase):
- ✅ **Бесплатно** - 1GB в Free tier
- ✅ **Быстро** - уже настроено
- ✅ **Независимо** - не привязано к Timeweb
- ✅ **Глобально** - работает из любой страны

### Для клиентов (Timeweb):
- ✅ **Российский хостинг** - данные в РФ
- ✅ **Быстрый доступ** - серверы в России
- ✅ **Соответствие 152-ФЗ** - для клиентов важно
- ✅ **Масштабируемость** - платные тарифы

---

## 🔧 КАК ЭТО РАБОТАЕТ В КОДЕ

Ваш проект автоматически определяет какое хранилище использовать:

```typescript
// Если указан NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET - используем Supabase
if (process.env.NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET) {
  // Загрузка в Supabase Storage
  const { data, error } = await supabase.storage
    .from(process.env.NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET)
    .upload(filePath, file);
}
// Иначе используем S3
else if (process.env.S3_BUCKET_NAME) {
  // Загрузка в Timeweb S3
  await s3Client.send(new PutObjectCommand({
    Bucket: process.env.S3_BUCKET_NAME,
    Key: filePath,
    Body: file
  }));
}
```

---

## 📊 СРАВНЕНИЕ ХРАНИЛИЩ

| Параметр | Supabase Storage | Timeweb S3 |
|----------|------------------|------------|
| **Бесплатный план** | 1 GB | Нет |
| **Стоимость** | $0.021/GB после 1GB | От 150₽/мес |
| **Расположение** | США/Европа | Россия |
| **152-ФЗ** | ❌ | ✅ |
| **Скорость в РФ** | Средняя | Высокая |
| **Для презентации** | ✅ Идеально | ❌ Платно |
| **Для клиентов РФ** | ⚠️ Можно | ✅ Лучше |

---

## 🎓 РЕКОМЕНДАЦИИ ДЛЯ ПРОДАЖИ

### При презентации клиенту:

**Если клиент из России:**
```
"Система поддерживает хранение файлов в российских дата-центрах 
(Timeweb Cloud) для соответствия 152-ФЗ. Для демонстрации используется 
международное хранилище, но при внедрении подключим российское."
```

**Если клиент международный:**
```
"Система использует Supabase Storage - надежное облачное хранилище 
с глобальной CDN для быстрого доступа из любой точки мира."
```

### В коммерческом предложении:

**Базовый пакет:**
- Supabase Storage (включено)
- До 1 GB файлов

**Расширенный пакет (для РФ):**
- Timeweb Cloud S3 (+ 150₽/мес)
- Хранение данных в РФ
- Соответствие 152-ФЗ
- Безлимитный трафик

---

## 🆘 ЧАСТЫЕ ВОПРОСЫ

### Можно ли использовать оба хранилища одновременно?
Нет, система использует одно хранилище. Но переключение занимает 1 минуту - просто измените `.env.local`.

### Что будет со старыми фото при переключении?
Старые фото останутся в старом хранилище. Новые будут загружаться в новое. Можно написать скрипт миграции если нужно.

### Нужно ли менять код при переключении?
Нет! Код автоматически определяет какое хранилище использовать по переменным окружения.

### Можно ли использовать другие S3-совместимые хранилища?
Да! Можно использовать AWS S3, Yandex Object Storage, MinIO и любые другие S3-совместимые хранилища.

---

## ✅ ЧЕКЛИСТ НАСТРОЙКИ

Для презентационной копии:
- [ ] Создан bucket `uploads` в Supabase
- [ ] Включен Public access
- [ ] Настроена политика чтения
- [ ] Раскомментирован `NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET` в `.env.local`
- [ ] Перезапущен dev сервер

Для клиента на Timeweb:
- [ ] Создан bucket в Timeweb Cloud
- [ ] Получены ключи доступа
- [ ] Настроены переменные S3_* в `.env.local`
- [ ] Закомментирован `NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET`
- [ ] Перезапущен сервер

---

## 🎉 ГОТОВО!

Теперь ваша презентационная копия использует Supabase Storage, а для клиентов вы легко можете переключить на Timeweb S3.

**Это отличное конкурентное преимущество!** 🚀
