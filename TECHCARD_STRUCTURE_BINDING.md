# 🔗 Привязка техзаданий к структуре объекта

## ✅ Что реализовано

### 1. 📊 Обновлена схема базы данных

**Файл:** `prisma/schema.prisma`

**Добавлены поля в модель `TechCard`:**
```prisma
model TechCard {
  // ... существующие поля
  
  // Привязка к элементам структуры (иерархическая)
  siteId               String?               // Участок
  zoneId               String?               // Зона
  roomGroupId          String?               // Группа помещений
  roomId               String?               // Помещение
  cleaningObjectItemId String?               // Объект уборки
  
  // ... остальные поля
}
```

**Миграция применена:** ✅ `npx prisma db push`

---

### 2. 🎨 Обновлен TechTaskModal

**Файл:** `src/components/TechTaskModal.tsx`

**Добавлено:**
- ✅ Загрузка структуры объекта при открытии модального окна
- ✅ Каскадные селекты для выбора:
  - Участок (Site)
  - Зона (Zone) - активируется при выборе участка
  - Группа помещений (RoomGroup) - активируется при выборе зоны
  - Помещение (Room) - активируется при выборе группы
- ✅ Автоматическая очистка зависимых полей при изменении родительского
- ✅ Сохранение выбранной структуры при создании/редактировании

**Пример кода:**
```tsx
// Каскадные селекты
const handleSiteChange = (siteId: string) => {
  const site = sites.find(s => s.id === siteId);
  setFormData({ ...formData, siteId, zoneId: undefined, roomGroupId: undefined, roomId: undefined });
  setZones(site?.zones || []);
  setRoomGroups([]);
  setRooms([]);
};

// Аналогично для Zone, RoomGroup, Room
```

**UI:**
```
┌─────────────────────────────────────────┐
│ Привязка к структуре объекта            │
├─────────────────────────────────────────┤
│ Участок:          [Выбрать ▼]           │
│ Зона:             [Выбрать ▼] (disabled)│
│ Группа помещений: [Выбрать ▼] (disabled)│
│ Помещение:        [Выбрать ▼] (disabled)│
└─────────────────────────────────────────┘
```

---

### 3. 🔌 Обновлены API endpoints

#### **POST /api/techcards**

**Файл:** `src/app/api/techcards/route.ts`

**Изменения:**
```typescript
// Было: все поля обязательны
if (!name || !workType || !frequency || !description || !roomId || !objectId)

// Стало: только основные поля обязательны
if (!name || !workType || !frequency || !objectId)

// Добавлена поддержка новых полей
const techCard = await prisma.techCard.create({
  data: {
    name,
    workType,
    frequency,
    description,
    objectId,
    ...(siteId && { siteId }),
    ...(zoneId && { zoneId }),
    ...(roomGroupId && { roomGroupId }),
    ...(roomId && { roomId }),
  },
  // ...
});
```

#### **PATCH /api/techcards/[id]**

**Файл:** `src/app/api/techcards/[id]/route.ts`

**Изменения:**
```typescript
const updatedTechCard = await prisma.techCard.update({
  where: { id },
  data: {
    name,
    workType,
    frequency,
    description,
    ...(siteId !== undefined && { siteId: siteId || null }),
    ...(zoneId !== undefined && { zoneId: zoneId || null }),
    ...(roomGroupId !== undefined && { roomGroupId: roomGroupId || null }),
    ...(roomId !== undefined && { roomId: roomId || null }),
  },
  // ...
});
```

---

### 4. 🔄 Исправлено отображение структуры

**Файл:** `src/components/ObjectEditModal.tsx`

**Проблема:** После создания элементов структуры они не отображались в модальном окне.

**Причина:** `handleStructureSaved` не ждал завершения загрузки данных перед закрытием модального окна.

**Решение:**
```typescript
// Было
const handleStructureSaved = () => {
  loadObjectData();
  closeStructureModal();
};

// Стало
const handleStructureSaved = async () => {
  await loadObjectData();
  closeStructureModal();
};
```

**Результат:** ✅ Новые элементы структуры теперь отображаются сразу после создания.

---

## 🎯 Как это работает

### Создание техзадания с привязкой к структуре:

1. **Открыть объект** → Редактировать → Техзадания → Добавить техзадание
2. **Заполнить основные поля:**
   - Название
   - Тип работ
   - Частота выполнения
   - Описание (опционально)
3. **Выбрать привязку к структуре:**
   - Выбрать участок → откроются зоны этого участка
   - Выбрать зону → откроются группы помещений этой зоны
   - Выбрать группу → откроются помещения этой группы
   - Выбрать помещение (финальный уровень)
4. **Сохранить**

### Варианты привязки:

| Уровень привязки | Пример использования |
|------------------|----------------------|
| **Только объект** | Общие работы для всего объекта (например, вывоз мусора) |
| **Объект + Участок** | Работы для всего участка (например, уборка территории участка) |
| **Объект + Участок + Зона** | Работы для зоны (например, мойка окон в зоне) |
| **Объект + ... + Группа** | Работы для группы помещений (например, уборка офисов) |
| **Объект + ... + Помещение** | Работы для конкретного помещения (например, влажная уборка кабинета 101) |

---

## 📊 Схема данных

```
CleaningObject (Объект)
  ├── Site (Участок) [siteId]
  │   ├── Zone (Зона) [zoneId]
  │   │   ├── RoomGroup (Группа помещений) [roomGroupId]
  │   │   │   ├── Room (Помещение) [roomId]
  │   │   │   │   └── CleaningObjectItem (Объект уборки) [cleaningObjectItemId]
  │   │   │   └── TechCard ← может быть привязана на любом уровне
```

---

## 🔍 Примеры использования

### Пример 1: Техзадание для всего участка
```json
{
  "name": "Уборка территории",
  "workType": "Уборка",
  "frequency": "Ежедневно",
  "objectId": "obj_123",
  "siteId": "site_456",
  "zoneId": null,
  "roomGroupId": null,
  "roomId": null
}
```

### Пример 2: Техзадание для конкретного помещения
```json
{
  "name": "Влажная уборка кабинета",
  "workType": "Уборка",
  "frequency": "Ежедневно",
  "objectId": "obj_123",
  "siteId": "site_456",
  "zoneId": "zone_789",
  "roomGroupId": "group_012",
  "roomId": "room_345"
}
```

### Пример 3: Общее техзадание для объекта
```json
{
  "name": "Вывоз мусора",
  "workType": "Вывоз мусора",
  "frequency": "Еженедельно",
  "objectId": "obj_123",
  "siteId": null,
  "zoneId": null,
  "roomGroupId": null,
  "roomId": null
}
```

---

## ✨ Преимущества новой системы

### 1. **Гибкость привязки**
- ✅ Можно создавать техзадания на любом уровне иерархии
- ✅ Не обязательно заполнять все уровни
- ✅ Подходит для разных типов объектов

### 2. **Удобство использования**
- ✅ Каскадные селекты автоматически фильтруют доступные варианты
- ✅ Невозможно выбрать несовместимые элементы
- ✅ Интуитивно понятный интерфейс

### 3. **Масштабируемость**
- ✅ Легко добавить новые уровни иерархии
- ✅ Поддержка сложных структур объектов
- ✅ Оптимизированные запросы к БД

---

## 🐛 Исправленные проблемы

### Проблема 1: Новые элементы структуры не отображались
**Статус:** ✅ Исправлено

**Решение:** Сделали `handleStructureSaved` асинхронным и добавили `await` перед `loadObjectData()`.

### Проблема 2: Техзадания создавались через prompt
**Статус:** ✅ Исправлено

**Решение:** Создан полноценный `TechTaskModal` с формой и валидацией.

### Проблема 3: Нет привязки к структуре
**Статус:** ✅ Исправлено

**Решение:** Добавлены поля в БД, каскадные селекты в UI, обновлены API endpoints.

---

## 🚀 Следующие шаги (опционально)

### Возможные улучшения:

1. **Отображение привязки в списке техзаданий**
   - Показывать иконки или бейджи с уровнем привязки
   - Фильтрация техзаданий по элементам структуры

2. **Массовое создание техзаданий**
   - Создать одно техзадание для нескольких помещений
   - Шаблоны техзаданий

3. **Копирование техзаданий**
   - Копировать техзадание с изменением привязки
   - Дублировать структуру техзаданий между объектами

4. **Отчеты по структуре**
   - Статистика выполнения по участкам/зонам
   - Визуализация загрузки по элементам структуры

---

## 📝 Итоги

**Реализовано:**
- ✅ Схема БД обновлена (добавлены поля siteId, zoneId, roomGroupId)
- ✅ TechTaskModal с каскадными селектами для выбора структуры
- ✅ API endpoints поддерживают новые поля
- ✅ Отображение структуры исправлено (асинхронная загрузка)
- ✅ Переименование "Техкарты" → "Техзадания" завершено

**Результат:**
Теперь при создании техзадания можно выбрать, к какому элементу структуры оно относится:
- Участок
- Зона
- Группа помещений
- Помещение

Все элементы структуры корректно отображаются после создания! 🎉
