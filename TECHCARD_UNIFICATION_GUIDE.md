# 📋 Руководство по унификации техкарт

## ⚠️ ВАЖНО: О дубликатах и уникальных ID

### Почему НЕ нужно удалять дубликаты:

Каждая техкарта имеет уникальный `id`, который используется в связях:
- `Task.techCardId` - завершенные задачи
- `Room.techCards` - связь с помещениями
- Другие таблицы

**Если удалить техкарту → потеряются все связанные данные!**

### ✅ Правильный подход:

1. **Унифицируем названия и периодичность** (name, frequency)
2. **НЕ удаляем** строки с одинаковыми значениями
3. **Группировка в UI** будет работать правильно (по унифицированным значениям)

**Результат:**
- В БД останутся все строки (даже "дубликаты")
- Все связи сохранятся
- В UI задачи будут правильно группироваться

---

## 🚀 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Экспорт техкарт в Excel

```bash
npm run export-techcards
```

**Результат:**
- Создастся файл `scripts/techcards-export.csv`
- Содержит все техкарты с полями для редактирования

---

### Шаг 2: Редактирование в Excel

#### 2.1. Открытие файла

1. Откройте `scripts/techcards-export.csv` в Excel
2. Если кодировка неправильная:
   - Откройте через "Данные" → "Из текста/CSV"
   - Выберите кодировку UTF-8
   - Разделитель: Табуляция

#### 2.2. Структура файла

| Столбец | Описание | Можно редактировать? |
|---------|----------|---------------------|
| `id` | Уникальный ID техкарты | ❌ **НЕТ! КРИТИЧНО!** |
| `objectName` | Название объекта | ℹ️ Только для справки |
| `roomName` | Название помещения | ℹ️ Только для справки |
| `taskName` | Название задачи | ✅ **ДА** |
| `frequency` | Периодичность | ✅ **ДА** |
| `workType` | Тип работы | ✅ ДА |
| `description` | Описание | ✅ ДА |
| `objectId` | ID объекта | ❌ НЕТ |
| `roomId` | ID помещения | ❌ НЕТ |

#### 2.3. Что редактировать

**Унификация периодичности (frequency):**
```
❌ "DAILY"           → ✅ "Ежедневно"
❌ "ежедневно"       → ✅ "Ежедневно"
❌ "WEEKLY"          → ✅ "Еженедельно"
❌ "1 раз в неделю"  → ✅ "Еженедельно"
❌ "MONTHLY"         → ✅ "Ежемесячно"
❌ "1 раз в месяц"   → ✅ "Ежемесячно"
```

**Рекомендуемые значения:**
- `Ежедневно`
- `2 раза в день`
- `Еженедельно`
- `2 раза в неделю`
- `5 раз в неделю`
- `Ежемесячно`
- `4 раза в месяц`
- `2 раза в год`
- `По мере необходимости`

**Унификация названий (taskName):**
```
❌ "Мытье окон (открывающиеся фрамуги - с 2-х сторон...)"
✅ "Мытье окон"

❌ "Влажная уборка пола с дезинфекцией"
❌ "Влажная уборка пола"
✅ "Влажная уборка пола" (выберите один вариант)

❌ "уборка мусорных корзин"
✅ "Уборка мусорных корзин" (с заглавной буквы)
```

#### 2.4. Добавление нового объекта

**НЕ ДОБАВЛЯЙТЕ новые строки в этот файл!**

Для добавления нового объекта:
1. Используйте UI системы
2. Или создайте отдельный скрипт

---

### Шаг 3: Сохранение файла

#### 3.1. Сохранение в Excel

1. Нажмите "Файл" → "Сохранить как"
2. Выберите тип файла: **"CSV (разделитель - табуляция) (*.txt)"**
3. Имя файла: `techcards-import.csv`
4. Сохраните в папку `scripts/`

#### 3.2. Проверка

Откройте `techcards-import.csv` в текстовом редакторе и проверьте:
- Столбцы разделены табуляцией (не запятыми!)
- Первая строка - заголовки
- Все ID на месте

---

### Шаг 4: Импорт обратно в БД

```bash
npm run import-techcards
```

**Что произойдет:**
1. Скрипт прочитает `techcards-import.csv`
2. Обновит поля `name`, `frequency`, `workType`, `description`
3. **НЕ изменит** `id`, `objectId`, `roomId` (связи сохранятся!)
4. Выведет статистику обновлений

**Пример вывода:**
```
📊 ИТОГОВАЯ СТАТИСТИКА:
✅ Успешно обновлено: 2456
❌ Ошибок: 0
📝 Всего строк: 2456
```

---

## 🔍 Проверка результата

### В UI:

1. Откройте календарь задач
2. Проверьте группировку по периодичности
3. Убедитесь, что дубликаты объединились

### В БД (Prisma Studio):

```bash
npx prisma studio
```

1. Откройте таблицу `TechCard`
2. Проверьте поле `frequency` - должны быть унифицированные значения
3. Проверьте поле `name` - должны быть исправленные названия

---

## ⚠️ Что делать с дубликатами?

### Вопрос: "У меня 2 техкарты с одинаковыми данными, что делать?"

**Ответ: Ничего не делать!**

**Пример:**
```
Техкарта 1: id=abc, name="Мытье окон", frequency="Ежедневно", objectId=obj1
Техкарта 2: id=xyz, name="Мытье окон", frequency="Ежедневно", objectId=obj1
```

Это **НЕ проблема**, потому что:
1. В UI они будут в одной группе (по name + frequency)
2. Обе техкарты могут иметь разные связи с задачами
3. Удаление одной может сломать связи

### Если ДЕЙСТВИТЕЛЬНО нужно удалить дубли:

Создам отдельный скрипт для **безопасного** объединения:
1. Найдет настоящие дубликаты
2. Перенесет все связи на одну техкарту
3. Удалит пустые дубли

**Но это сложно и рискованно!** Рекомендую оставить как есть.

---

## 📊 Частые вопросы

### Q: Можно ли изменить `objectId` или `roomId`?
**A:** Нет! Это сломает связи. Используйте UI для перемещения техкарт.

### Q: Можно ли удалить строки из CSV?
**A:** Нет! Это удалит техкарты и все связанные данные.

### Q: Можно ли добавить новые строки в CSV?
**A:** Нет! Используйте UI или отдельный скрипт для создания.

### Q: Что делать, если импорт выдал ошибки?
**A:** Проверьте:
- Формат файла (CSV с табуляцией)
- Кодировка UTF-8
- Все ID корректны
- Нет лишних столбцов

---

## ✅ Чеклист

Перед импортом убедитесь:

- [ ] Экспортировали данные (`npm run export-techcards`)
- [ ] Отредактировали только `taskName` и `frequency`
- [ ] **НЕ изменили** столбец `id`
- [ ] **НЕ изменили** столбцы `objectId` и `roomId`
- [ ] **НЕ удалили** строки
- [ ] **НЕ добавили** новые строки
- [ ] Сохранили как CSV (табуляция)
- [ ] Переименовали в `techcards-import.csv`
- [ ] Поместили в папку `scripts/`
- [ ] Создали резервную копию БД (на всякий случай!)

---

## 🆘 Помощь

Если что-то пошло не так:
1. Проверьте логи выполнения скрипта
2. Откройте Prisma Studio и проверьте данные
3. Если нужен откат - восстановите БД из резервной копии
