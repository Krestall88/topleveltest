# ✅ Чеклист Telegram уведомлений

## 📱 Реализованные уведомления

### ✅ Для менеджеров:

| Событие | Статус | Файл | Описание |
|---------|--------|------|----------|
| **Новая задача (от клиента через бота)** | ✅ Реализовано | `src/app/api/webhooks/telegram/route.ts` | Когда клиент отправляет задание через Telegram бота |
| **Новая задача (от админа через веб)** | ✅ Реализовано | `src/app/api/additional-tasks/route.ts` | Когда админ создаёт задание через веб-интерфейс |
| **Комментарий к задаче** | ✅ Реализовано | `src/app/api/additional-tasks/[id]/comments/route.ts` | Когда кто-то оставляет комментарий к задаче менеджера |
| **Назначение на объект** | ✅ Реализовано | `src/app/api/objects/[id]/assign-manager/route.ts` | Когда менеджера назначают на объект |
| **Назначение на участок** | ✅ Реализовано | `src/app/api/sites/[id]/assign-manager/route.ts` | Когда менеджера назначают на участок |

### ⚠️ Дополнительные события (можно добавить):

| Событие | Статус | Приоритет | Описание |
|---------|--------|-----------|----------|
| **Просроченная задача** | ❌ Не реализовано | Средний | Напоминание о незавершенной задаче (требует cron job) |
| **Изменение статуса задачи** | ❌ Не реализовано | Низкий | Когда задача взята в работу/завершена |
| **Смена роли** | ❌ Не реализовано | Низкий | Когда пользователю изменили роль |
| **Снятие с объекта** | ❌ Не реализовано | Низкий | Когда менеджера сняли с объекта |

---

## 🌐 Web Push уведомления (в браузере)

### Требование:
> "эти же уведомления должны отображаться когда программа открыта на компьютере в браузере, но при этом открыта другая вкладка или свернуто окно браузера"

### Статус: ❌ Не реализовано

### Что нужно сделать:

#### 1. **Использовать Browser Notifications API**

Это позволит показывать уведомления даже когда:
- Браузер свернут
- Открыта другая вкладка
- Пользователь работает в другом приложении

#### 2. **Технологии:**

**Вариант A: Polling + Browser Notifications (проще)**
- Каждые 30 секунд проверять новые события
- Показывать Browser Notification
- Не требует Service Worker

**Вариант B: Server-Sent Events (SSE)**
- Реал-тайм подключение к серверу
- Сервер пушит события клиенту
- Более эффективно

**Вариант C: WebSocket**
- Двусторонняя связь
- Самый сложный вариант
- Требует WebSocket сервер

#### 3. **Рекомендация: Вариант A (Polling + Browser Notifications)**

**Преимущества:**
- ✅ Простая реализация
- ✅ Не требует дополнительной инфраструктуры
- ✅ Работает везде
- ✅ У вас уже есть polling для задач

**Что нужно добавить:**
1. Запросить разрешение на уведомления у пользователя
2. При обнаружении новых событий показывать Browser Notification
3. При клике на уведомление - открывать нужную страницу

---

## 📋 План реализации Browser Notifications

### Шаг 1: Создать хук для уведомлений

```typescript
// src/hooks/useBrowserNotifications.ts
export function useBrowserNotifications() {
  const [permission, setPermission] = useState<NotificationPermission>('default');
  
  useEffect(() => {
    if ('Notification' in window) {
      setPermission(Notification.permission);
    }
  }, []);
  
  const requestPermission = async () => {
    const result = await Notification.requestPermission();
    setPermission(result);
    return result;
  };
  
  const showNotification = (title: string, options?: NotificationOptions) => {
    if (permission === 'granted') {
      new Notification(title, options);
    }
  };
  
  return { permission, requestPermission, showNotification };
}
```

### Шаг 2: Интегрировать в AppLayout

```typescript
// В AppLayout.tsx
const { showNotification } = useBrowserNotifications();

// При обнаружении новых событий:
useEffect(() => {
  if (newTasksCount > previousCount) {
    showNotification('Новое задание', {
      body: 'У вас новое дополнительное задание',
      icon: '/icon.png',
      tag: 'new-task',
      requireInteraction: false
    });
  }
}, [newTasksCount]);
```

### Шаг 3: Добавить API для получения событий

```typescript
// GET /api/notifications/unread
// Возвращает список непрочитанных событий для пользователя
```

### Шаг 4: Добавить UI для запроса разрешения

На странице `/telegram` добавить:
- Кнопку "Включить уведомления в браузере"
- Статус разрешения
- Тестовое уведомление

---

## 🔔 Типы Browser Notifications

### 1. **Новая задача**
```javascript
new Notification('📋 Новое задание', {
  body: 'Объект: Нектар\nЗадача: Проверить уборку',
  icon: '/icons/task.png',
  badge: '/icons/badge.png',
  tag: 'task-123',
  data: { taskId: '123', url: '/additional-tasks' }
});
```

### 2. **Новый комментарий**
```javascript
new Notification('💬 Новый комментарий', {
  body: 'Админ оставил комментарий к вашей задаче',
  icon: '/icons/comment.png',
  tag: 'comment-456'
});
```

### 3. **Назначение на объект**
```javascript
new Notification('🏢 Новое назначение', {
  body: 'Вы назначены менеджером объекта "Нектар"',
  icon: '/icons/building.png',
  tag: 'assignment-789'
});
```

---

## 🎯 Приоритеты реализации

### Высокий приоритет:
1. ✅ **Telegram уведомления** - реализовано
2. 🔄 **Browser Notifications для новых заданий** - нужно реализовать
3. 🔄 **Browser Notifications для комментариев** - нужно реализовать

### Средний приоритет:
4. ⏰ **Просроченные задачи** (требует cron job)
5. 📊 **Еженедельные отчеты**

### Низкий приоритет:
6. 🔄 **Изменение статуса задачи**
7. 👤 **Смена роли**

---

## 📝 Следующие шаги

### Для Telegram уведомлений:
1. ✅ Применить миграцию `npx prisma db push`
2. ✅ Перезапустить сервер
3. ✅ Протестировать все события

### Для Browser Notifications:
1. ❌ Создать хук `useBrowserNotifications`
2. ❌ Добавить API `/api/notifications/unread`
3. ❌ Интегрировать в `AppLayout`
4. ❌ Добавить UI на странице `/telegram`
5. ❌ Протестировать на разных браузерах

---

## 🧪 Тестирование

### Telegram уведомления:
- [x] Создание задания от админа
- [ ] Создание задания от клиента через бота
- [ ] Добавление комментария
- [ ] Назначение на объект
- [ ] Назначение на участок

### Browser Notifications:
- [ ] Запрос разрешения
- [ ] Показ уведомления при новой задаче
- [ ] Показ уведомления при комментарии
- [ ] Клик по уведомлению открывает нужную страницу
- [ ] Работает когда браузер свернут
- [ ] Работает когда открыта другая вкладка
