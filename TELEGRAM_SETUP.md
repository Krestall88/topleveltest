# 🤖 Настройка Telegram бота для дополнительных заданий

## Шаг 1: Создание бота

1. **Откройте Telegram** и найдите бота [@BotFather](https://t.me/botfather)

2. **Отправьте команду** `/newbot`

3. **Введите название бота** (например: "Клининг Задания")

4. **Введите username бота** (например: "cleaning_tasks_bot")

5. **Сохраните токен бота** - он понадобится для настройки

## Шаг 2: Настройка переменных окружения

Добавьте в файл `.env.local`:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
NEXT_PUBLIC_BASE_URL=http://localhost:3001
```

## Шаг 3: Настройка webhook

1. **Запустите ваш сервер** на доступном URL (например, через ngrok для тестирования)

2. **Установите webhook** выполнив GET запрос:
```
https://api.telegram.org/bot{YOUR_BOT_TOKEN}/setWebhook?url={YOUR_DOMAIN}/api/webhooks/telegram
```

Пример:
```
https://api.telegram.org/bot123456789:ABCDEF.../setWebhook?url=https://yourdomain.com/api/webhooks/telegram
```

## Шаг 4: Тестирование

1. **Найдите вашего бота** в Telegram по username

2. **Отправьте команду** `/start`

3. **Бот должен показать список объектов** с inline кнопками

4. **Нажмите на кнопку** с названием нужного объекта

5. **Бот подтвердит выбор** и покажет информацию об объекте

6. **Отправьте тестовое сообщение** - оно должно создать дополнительное задание

## Шаг 5: Проверка работы

1. **Войдите в систему** как менеджер объекта

2. **Перейдите в раздел** "Доп. задания" или на страницу менеджера

3. **Проверьте**, что задание появилось в списке

## Возможные проблемы

### Webhook не работает
- Проверьте, что URL доступен извне
- Убедитесь, что используется HTTPS (для продакшена)
- Проверьте логи сервера на ошибки

### Бот не отвечает
- Проверьте правильность токена
- Убедитесь, что webhook установлен корректно
- Проверьте переменные окружения

### Задания не создаются
- Проверьте, что клиент привязан к объекту
- Убедитесь, что у объекта назначен менеджер
- Проверьте логи API в консоли

## Команды для отладки

### Проверить статус webhook:
```
https://api.telegram.org/bot{YOUR_BOT_TOKEN}/getWebhookInfo
```

### Удалить webhook:
```
https://api.telegram.org/bot{YOUR_BOT_TOKEN}/deleteWebhook
```

### Получить обновления (для тестирования без webhook):
```
https://api.telegram.org/bot{YOUR_BOT_TOKEN}/getUpdates
```

## Дополнительные настройки

### Команды бота
Вы можете настроить команды бота через @BotFather:
- `/start` - Начать работу с ботом
- `/help` - Помощь по использованию
- `/status` - Проверить статус привязки

### Описание бота
Установите описание через @BotFather:
"Бот для отправки дополнительных заданий по уборке. Отправляйте текст, фото или голосовые сообщения - они автоматически попадут к ответственному менеджеру."

## Особенности нового интерфейса

### Inline кнопки для выбора объектов
- После команды `/start` бот показывает список всех доступных объектов
- Каждый объект представлен отдельной кнопкой с названием
- Нажатие на кнопку автоматически привязывает пользователя к объекту
- Исходное сообщение редактируется и показывает выбранный объект

### Преимущества нового подхода
- ✅ **Удобство**: не нужно переходить по внешним ссылкам
- ✅ **Скорость**: выбор объекта в один клик
- ✅ **Наглядность**: все объекты видны сразу
- ✅ **Мобильность**: отлично работает на телефонах

### Что происходит после выбора
1. Бот сохраняет привязку пользователя к объекту
2. Показывает подтверждение с информацией об объекте
3. Все последующие сообщения автоматически создают задания для выбранного объекта
