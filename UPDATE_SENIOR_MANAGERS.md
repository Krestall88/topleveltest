# 🔧 ОБНОВЛЕНИЕ: ДОБАВЛЕНИЕ СТАРШИХ МЕНЕДЖЕРОВ

## ✅ ЧТО СДЕЛАНО:

### 1. **Исправлена проблема "участок → техзадание"**
- Виртуальные уровни теперь создаются ВСЕГДА, даже если есть только техзадание
- Исправлены условия в строках 148 и 181 файла `comprehensive-upload/route.ts`

### 2. **Добавлена привязка менеджеров к участкам**
- Чтение колонки "менеджер участка" из Excel
- Автоматическая привязка менеджера к участку при импорте
- Подробное логирование процесса

### 3. **Добавлена роль SENIOR_MANAGER**
- Обновлен enum Role в `prisma/schema.prisma`
- Добавлена поддержка старших менеджеров в функции поиска
- Реализована привязка старших менеджеров к участкам

### 4. **Обработка старших менеджеров из Excel**
- Чтение колонок "старший менеджер 1" и "старший менеджер 2"
- Автоматическая привязка к участкам
- Если указаны оба старших менеджера, первый сохраняется в `seniorManagerId`, второй упоминается в комментарии

---

## ⚠️ ТРЕБУЕТСЯ ВЫПОЛНИТЬ:

### ШАГ 1: Обновить базу данных

```powershell
# Применить изменения схемы к БД
npx prisma db push

# Сгенерировать новый Prisma Client
npx prisma generate
```

**Ожидаемый результат:**
```
✔ Generated Prisma Client
```

---

### ШАГ 2: Перезапустить сервер разработки

```powershell
# Остановите текущий сервер (Ctrl+C)
# Запустите заново
npm run dev
```

---

## 📝 СЛЕДУЮЩИЕ ШАГИ (после обновления БД):

1. **Обновить UI создания/редактирования менеджеров**
   - Добавить выбор роли (MANAGER / SENIOR_MANAGER)
   - Обновить форму в `/managers`

2. **Реализовать логику прав доступа**
   - Старший менеджер видит все объекты своих подчиненных
   - Может закрывать задачи, но ответственность на подчиненных

3. **Протестировать импорт**
   - Загрузить Excel с менеджерами участков
   - Загрузить Excel со старшими менеджерами
   - Проверить корректность привязок

---

## 📊 СТРУКТУРА ДАННЫХ:

### Колонки Excel для импорта:

| Колонка | Описание | Обязательна |
|---------|----------|-------------|
| участок | Название участка | Нет |
| зона | Название зоны | Нет |
| группа помещений | Название группы | Нет |
| помещение | Название помещения | Нет |
| Объект уборки | Название объекта уборки | Нет |
| тех задание | Название техзадания | **Да** |
| периодичность | Периодичность выполнения | Нет |
| **менеджер участка** | ФИО менеджера участка | Нет |
| **старший менеджер 1** | ФИО первого старшего менеджера | Нет |
| **старший менеджер 2** | ФИО второго старшего менеджера | Нет |

### Логика привязки менеджеров:

1. **Менеджер объекта** - читается из колонки "Менеджер объекта ФИО", привязывается к `CleaningObject.managerId`
2. **Менеджер участка** - читается из колонки "менеджер участка", привязывается к `Site.managerId`
3. **Старший менеджер** - читается из колонок "старший менеджер 1/2", привязывается к `Site.seniorManagerId`

---

## 🔍 ПРОВЕРКА РАБОТЫ:

После обновления БД и перезапуска сервера:

1. **Загрузите Excel** с данными ООО "ЖилЭнерго" и ООО "ЖЭУ-66"
2. **Проверьте логи сервера** - должны появиться сообщения:
   ```
   👤 Найден менеджер участка: [ИМЯ] для участка [УЧАСТОК]
   👔 Найден старший менеджер 1: [ИМЯ] для участка [УЧАСТОК]
   ✅ Создан участок: [УЧАСТОК] (менеджер: [ИМЯ], ст.менеджер: [ИМЯ])
   ```

3. **Проверьте структуру** - должна создаться иерархия "участок → техзадание" с виртуальными уровнями

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ:

### Ошибки типов TypeScript
После обновления схемы появятся ошибки:
```
Type '"SENIOR_MANAGER"' is not assignable to type 'Role'
```

**Решение:** Запустите `npx prisma generate` - это обновит типы Prisma Client.

---

## 📞 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

1. Проверьте, что БД обновлена: `npx prisma db push`
2. Проверьте, что Prisma Client обновлен: `npx prisma generate`
3. Перезапустите сервер: `npm run dev`
4. Проверьте логи сервера при импорте
5. Проверьте консоль браузера на ошибки
