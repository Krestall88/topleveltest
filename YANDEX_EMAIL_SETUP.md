# 📧 НАСТРОЙКА ЯНДЕКС ПОЧТЫ ДЛЯ СИСТЕМЫ КЛИНИНГА

## 🎯 Цель настройки

Настроить автоматический сбор дополнительных заданий от клиентов через email, аналогично Telegram боту:
- Клиенты пишут на email → система создает задания
- Автоматическая привязка к объектам
- Уведомления менеджерам

## 📋 Что потребуется

1. **Яндекс почта** для приема писем (например: `dop@yourdomain.ru`)
2. **Пароль приложения** для доступа к почте
3. **Настройка переменных окружения**
4. **Webhook для обработки писем**

## 🔧 Пошаговая настройка

### **Шаг 1: Создание почтового ящика**

1. **Создайте почтовый ящик** на Яндекс:
   - Перейдите на `mail.yandex.ru`
   - Создайте новый ящик: `dop@yourdomain.ru` (или используйте существующий)

2. **Включите IMAP доступ:**
   - Зайдите в **Настройки** → **Почтовые программы**
   - Включите **IMAP доступ**
   - Включите **SMTP доступ**

### **Шаг 2: Создание пароля приложения**

1. **Перейдите в настройки безопасности:**
   - Откройте `passport.yandex.ru`
   - Войдите в свой аккаунт
   - Перейдите в **Безопасность**

2. **Создайте пароль приложения:**
   - Найдите раздел **Пароли приложений**
   - Нажмите **Создать пароль приложения**
   - Выберите **Почта**
   - Введите название: `Cleaning System`
   - **Сохраните полученный пароль!** (он понадобится в настройках)

### **Шаг 3: Настройка переменных окружения**

Добавьте в файл `.env` (в кавычках, как другие настройки):

```env
# Яндекс Email настройки
EMAIL_HOST="imap.yandex.ru"
EMAIL_PORT="993"
EMAIL_SECURE="true"
EMAIL_USER="dop@yourdomain.ru"
EMAIL_PASSWORD="your_app_password_here"

# SMTP для отправки ответов
SMTP_HOST="smtp.yandex.ru"
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER="dop@yourdomain.ru"
SMTP_PASSWORD="your_app_password_here"
```

**Замените:**
- `dop@yourdomain.ru` → ваш email
- `your_app_password_here` → пароль приложения из Шага 2
- `https://your-domain.com` → ваш домен (или ngrok URL)

### **Шаг 4: Проверка API эндпоинтов**

Система уже имеет готовые API для работы с email:

#### **1. Webhook для обработки писем:** `/api/webhooks/email`
- Автоматически обрабатывает входящие письма
- Создает дополнительные задания
- Сохраняет вложения

#### **2. API привязки клиентов:** `/api/client-bindings`
- Привязка email к объектам
- Управление привязками

#### **3. Страница выбора объекта:** `/choose-object`
- Веб-интерфейс для выбора объекта
- Поддержка параметра `?email=client@example.com`

### **Шаг 5: Настройка автоматического чтения почты**

Создайте скрипт для периодической проверки почты:

```javascript
// email-checker.js
const { PrismaClient } = require('@prisma/client');
const Imap = require('imap');
const { simpleParser } = require('mailparser');

const prisma = new PrismaClient();

const imapConfig = {
  user: process.env.EMAIL_USER,
  password: process.env.EMAIL_PASSWORD,
  host: process.env.EMAIL_HOST,
  port: parseInt(process.env.EMAIL_PORT || '993'),
  tls: process.env.EMAIL_SECURE === 'true',
  tlsOptions: { rejectUnauthorized: false }
};

function checkEmail() {
  const imap = new Imap(imapConfig);
  
  imap.once('ready', () => {
    console.log('📧 Подключение к почте установлено');
    
    imap.openBox('INBOX', false, (err, box) => {
      if (err) throw err;
      
      // Ищем непрочитанные письма
      imap.search(['UNSEEN'], (err, results) => {
        if (err) throw err;
        
        if (results.length === 0) {
          console.log('📭 Новых писем нет');
          imap.end();
          return;
        }
        
        console.log(`📬 Найдено ${results.length} новых писем`);
        
        const fetch = imap.fetch(results, { bodies: '' });
        
        fetch.on('message', (msg) => {
          msg.on('body', (stream) => {
            simpleParser(stream, async (err, parsed) => {
              if (err) throw err;
              
              // Обрабатываем письмо
              await processEmail(parsed);
            });
          });
          
          msg.once('attributes', (attrs) => {
            // Помечаем как прочитанное
            imap.addFlags(attrs.uid, ['\\Seen'], (err) => {
              if (err) console.error('Ошибка пометки письма:', err);
            });
          });
        });
        
        fetch.once('end', () => {
          console.log('✅ Обработка писем завершена');
          imap.end();
        });
      });
    });
  });
  
  imap.once('error', (err) => {
    console.error('❌ Ошибка IMAP:', err);
  });
  
  imap.connect();
}

async function processEmail(email) {
  try {
    console.log(`📧 Обрабатываем письмо от: ${email.from.text}`);
    console.log(`📋 Тема: ${email.subject}`);
    
    const senderEmail = email.from.value[0].address;
    
    // Проверяем, есть ли привязка к объекту
    const binding = await prisma.clientBinding.findFirst({
      where: { email: senderEmail },
      include: { object: { include: { manager: true } } }
    });
    
    if (!binding) {
      console.log('🔗 Клиент не привязан к объекту, отправляем ссылку для выбора');
      await sendObjectSelectionEmail(senderEmail);
      return;
    }
    
    // Создаем дополнительное задание
    const task = await prisma.additionalTask.create({
      data: {
        title: email.subject || 'Задание по email',
        description: email.text || email.html || '',
        source: 'EMAIL',
        sourceData: {
          from: email.from.text,
          subject: email.subject,
          date: email.date
        },
        objectId: binding.objectId,
        assignedToId: binding.object.managerId,
        status: 'NEW'
      }
    });
    
    console.log(`✅ Создано задание #${task.id} для объекта ${binding.object.name}`);
    
    // Отправляем подтверждение
    await sendConfirmationEmail(senderEmail, binding.object, task);
    
  } catch (error) {
    console.error('❌ Ошибка обработки письма:', error);
  }
}

async function sendObjectSelectionEmail(email) {
  // Отправляем письмо с ссылкой на выбор объекта
  const baseUrl = process.env.NEXT_PUBLIC_BASE_URL;
  const selectionUrl = `${baseUrl}/choose-object?email=${encodeURIComponent(email)}`;
  
  // Здесь код отправки email через SMTP
  console.log(`📤 Отправляем ссылку выбора объекта: ${selectionUrl}`);
}

async function sendConfirmationEmail(email, object, task) {
  // Отправляем подтверждение о создании задания
  console.log(`📤 Отправляем подтверждение о создании задания #${task.id}`);
}

// Запускаем проверку каждые 5 минут
setInterval(checkEmail, 5 * 60 * 1000);
console.log('🚀 Запущена проверка почты каждые 5 минут');

// Первая проверка сразу
checkEmail();
```

### **Шаг 6: Установка зависимостей**

Установите необходимые пакеты:

```bash
npm install imap mailparser nodemailer
```

### **Шаг 6.1: Проверка установки**

Убедитесь, что все пакеты установлены:

```bash
npm list imap mailparser nodemailer
```

### **Шаг 7: Запуск системы**

1. **Запустите основной сервер:**
```bash
npm run dev
```

2. **Запустите проверку почты:**
```bash
node email-checker.js
```

## 🎯 Логика работы

### **1. Первое письмо от клиента:**
- Клиент пишет на `dop@yourdomain.ru`
- Система проверяет привязку к объекту
- Если привязки нет → отправляет ссылку для выбора объекта
- Клиент переходит по ссылке и выбирает объект

### **2. Последующие письма:**
- Все письма автоматически создают дополнительные задания
- Задания назначаются менеджеру выбранного объекта
- Клиент получает подтверждение

### **3. Обработка менеджером:**
- Менеджер видит задания в интерфейсе `/manager`
- Может взять в работу и выполнить
- Система отправляет уведомления клиенту

## 📱 Интеграция с существующей системой

Система email полностью интегрирована с:
- ✅ **Дополнительными заданиями** - все письма становятся заданиями
- ✅ **Привязкой клиентов** - единая система для Telegram и Email
- ✅ **Интерфейсом менеджера** - задания отображаются вместе
- ✅ **Аналитикой** - статистика по всем источникам

## 🔧 Настройка домена (опционально)

Для профессионального использования:

1. **Настройте MX записи** для вашего домена
2. **Создайте почтовый ящик** `dop@yourdomain.ru`
3. **Обновите переменные окружения**

## ✅ Проверка работы

1. **Отправьте тестовое письмо** на настроенный email
2. **Проверьте логи** email-checker.js
3. **Убедитесь**, что создалось дополнительное задание
4. **Проверьте интерфейс менеджера**

## 🎉 Результат

После настройки клиенты смогут:
- ✅ **Писать на email** вместо Telegram
- ✅ **Выбирать объект** через веб-интерфейс
- ✅ **Получать подтверждения** о создании заданий
- ✅ **Отправлять вложения** (фото, документы)

**Система будет работать параллельно с Telegram ботом!** 🚀
