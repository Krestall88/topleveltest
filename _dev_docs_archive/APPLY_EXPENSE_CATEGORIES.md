# 🚀 Применение системы статей расходов - Финальная инструкция

## ✅ Что готово к применению

### Backend (100% готово):
- ✅ Схема БД обновлена
- ✅ 8 API endpoints созданы
- ✅ Проверка лимитов реализована
- ✅ Статистика работает
- ✅ Seed-скрипт готов

### Frontend (100% готово):
- ✅ Страница управления категориями
- ✅ Компонент настройки лимитов
- ✅ Карточка статистики по категории
- ✅ Интеграция в меню
- ✅ Обновлен API расходов

## 📋 Пошаговое применение

### Шаг 1: Применить миграцию БД

```bash
cd c:\Users\Тайм\Documents\toplevel

# Применить миграцию
npx prisma migrate deploy
```

**Ожидаемый результат:**
- Создана таблица `ExpenseCategory`
- Создана таблица `ExpenseCategoryLimit`
- Обновлена таблица `InventoryExpense` (добавлено поле `categoryId`)
- Создан enum `ExpensePeriodType`

### Шаг 2: Сгенерировать Prisma Client

```bash
npx prisma generate
```

**Ожидаемый результат:**
- Все lint ошибки исчезнут
- Prisma Client обновлен с новыми моделями
- TypeScript типы сгенерированы

### Шаг 3: Создать начальные категории

```bash
npx ts-node prisma/seeds/expense-categories.ts
```

**Ожидаемый результат:**
- Создано 10 категорий:
  1. Химия, инвентарь, расходники
  2. Тележки
  3. Спецодежда
  4. Покупка оборудования
  5. Ремонт оборудования
  6. Медицинские книжки
  7. ГСМ
  8. Вызов техники в зимний период
  9. Реагенты
  10. Диспенсеры

### Шаг 4: Перезапустить dev server

```bash
npm run dev
```

### Шаг 5: Проверить работу

#### 1. Управление категориями (Админ):
```
http://localhost:3000/admin/expense-categories
```

**Что проверить:**
- ✅ Отображается список категорий
- ✅ Можно создать новую категорию
- ✅ Можно редактировать категорию
- ✅ Можно активировать/деактивировать
- ✅ Нельзя удалить категорию с расходами

#### 2. Настройка лимитов (Админ):
```
Открыть любой объект → вкладка "Лимиты расходов"
```

**Что проверить:**
- ✅ Отображается список лимитов
- ✅ Можно создать лимит для категории
- ✅ Выбор типа периода работает
- ✅ Для MONTHLY показываются месяц/год
- ✅ Для SEMI_ANNUAL/ANNUAL показываются даты
- ✅ Можно редактировать лимит
- ✅ Можно удалить лимит

#### 3. Добавление расхода с категорией:
```
API: POST /api/inventory/expenses
Body: {
  "objectId": "...",
  "amount": 1000,
  "description": "Тест",
  "categoryId": "...",
  "month": 11,
  "year": 2025
}
```

**Что проверить:**
- ✅ Расход создается с категорией
- ✅ Если лимит превышен - возвращается ошибка
- ✅ Если использовано >90% - возвращается предупреждение

#### 4. Статистика по категориям:
```
API: GET /api/expenses/stats/[objectId]?month=11&year=2025
```

**Что проверить:**
- ✅ Возвращается разбивка по категориям
- ✅ Показываются лимиты и остатки
- ✅ Процент использования считается правильно
- ✅ Расходы без категории отдельно

## 🎯 Примеры использования

### Пример 1: Настройка лимитов для объекта "Офис А"

**Химия - ежедневная закупка:**
```
Категория: Химия, инвентарь, расходники
Тип периода: Ежедневно
Сумма: 1000 ₽
Повторяющийся: Да
```

**Химия - генеральная уборка:**
```
Категория: Химия, инвентарь, расходники
Тип периода: Раз в 6 месяцев
Сумма: 15000 ₽
Дата начала: 01.01.2025
Дата окончания: 30.06.2025
```

**Спецодежда - ежемесячно:**
```
Категория: Спецодежда
Тип периода: Ежемесячно
Сумма: 5000 ₽
Месяц: Ноябрь
Год: 2025
Повторяющийся: Да
```

### Пример 2: Добавление расхода менеджером

**Через API:**
```bash
curl -X POST http://localhost:3000/api/inventory/expenses \
  -H "Content-Type: application/json" \
  -d '{
    "objectId": "obj_123",
    "categoryId": "cat_chemistry",
    "amount": 500,
    "description": "Моющее средство",
    "month": 11,
    "year": 2025
  }'
```

**Ответ при успехе:**
```json
{
  "id": "exp_123",
  "amount": 500,
  "description": "Моющее средство",
  "category": {
    "id": "cat_chemistry",
    "name": "Химия, инвентарь, расходники"
  },
  ...
}
```

**Ответ при превышении лимита:**
```json
{
  "message": "Expense exceeds limit",
  "warning": "Превышен лимит \"DAILY\". Лимит: 1000, Текущие расходы: 800, Остаток: 200",
  "limitExceeded": true
}
```

## 📁 Созданные файлы

### Backend:
```
prisma/
  schema.prisma (обновлена)
  seeds/
    expense-categories.ts

src/app/api/
  expense-categories/
    route.ts
    [id]/route.ts
  objects/[id]/
    expense-limits/route.ts
  expense-limits/[id]/route.ts
  objects/[id]/expenses/route.ts (обновлена)
  expenses/stats/[objectId]/route.ts
  inventory/expenses/route.ts (обновлена)
```

### Frontend:
```
src/app/admin/
  expense-categories/page.tsx

src/components/
  ExpenseCategoriesManager.tsx
  ObjectExpenseLimitsSettings.tsx
  ExpenseCategoryStatsCard.tsx
  AppLayout.tsx (обновлена)
```

### Документация:
```
EXPENSE_CATEGORIES_COMPLETE.md
EXPENSE_CATEGORIES_MIGRATION.md
EXPENSE_CATEGORIES_UI_INTEGRATION.md
APPLY_EXPENSE_CATEGORIES.md (этот файл)
docs/EXPENSE_CATEGORIES_GUIDE.md
```

## ⚠️ Важные моменты

### 1. Обратная совместимость
- ✅ Старые расходы без категории продолжают работать
- ✅ Поле `categoryId` опциональное
- ✅ Старая модель `InventoryLimit` сохранена

### 2. Права доступа

**Администратор:**
- ✅ Управление категориями
- ✅ Настройка лимитов
- ✅ Просмотр всех расходов

**Заместитель администратора:**
- ✅ Управление категориями
- ✅ Настройка лимитов
- ✅ Просмотр расходов по назначенным объектам

**Менеджер:**
- ❌ Управление категориями
- ❌ Настройка лимитов
- ✅ Добавление расходов по своим объектам
- ✅ Просмотр расходов по своим объектам

**Бухгалтер:**
- ❌ Управление категориями
- ❌ Настройка лимитов
- ✅ Просмотр всех расходов

### 3. Проверка лимитов

**Автоматическая проверка при:**
- Добавлении расхода через `/api/objects/:id/expenses`

**Типы проверок:**
- DAILY - сумма расходов за текущий день
- MONTHLY - сумма расходов за указанный месяц
- SEMI_ANNUAL/ANNUAL - сумма расходов за период

**Результаты:**
- Лимит не превышен → расход создается
- Использовано >90% → расход создается + предупреждение
- Лимит превышен → ошибка 400

## 🐛 Troubleshooting

### Проблема: Lint ошибки после создания файлов

**Решение:**
```bash
npx prisma generate
```

### Проблема: Миграция не применяется

**Решение:**
```bash
# Проверить статус миграций
npx prisma migrate status

# Сбросить БД (ОСТОРОЖНО! Удалит все данные)
npx prisma migrate reset

# Применить миграцию заново
npx prisma migrate deploy
```

### Проблема: Категории не создаются

**Решение:**
```bash
# Проверить что миграция применена
npx prisma migrate status

# Запустить seed вручную
npx ts-node prisma/seeds/expense-categories.ts
```

### Проблема: API возвращает 500

**Решение:**
1. Проверить что Prisma Client сгенерирован
2. Проверить логи сервера
3. Проверить что миграция применена

## ✅ Чеклист готовности

- [ ] Миграция применена (`npx prisma migrate deploy`)
- [ ] Prisma Client сгенерирован (`npx prisma generate`)
- [ ] Начальные категории созданы (`npx ts-node prisma/seeds/expense-categories.ts`)
- [ ] Dev server перезапущен
- [ ] Страница категорий открывается
- [ ] Можно создать категорию
- [ ] Можно создать лимит для объекта
- [ ] Можно добавить расход с категорией
- [ ] Проверка лимитов работает
- [ ] Статистика отображается

## 🎉 Готово!

После выполнения всех шагов система статей расходов полностью работает:

✅ **10 предустановленных категорий**
✅ **Гибкие лимиты** (ежедневные, месячные, полугодовые, годовые)
✅ **Автоматическая проверка** при добавлении расхода
✅ **Детальная статистика** по категориям
✅ **Универсальность** - работает для любых объектов

**Решение задачи заказчика:**
- ✅ Статьи расходов вместо общих цифр
- ✅ Лимиты по статьям для каждого объекта
- ✅ Химия с двумя лимитами (ежедневный + генуборка)
- ✅ Детальное отслеживание остатков
- ✅ Общие цифры в сводке

---

**Вопросы?** Смотрите полную документацию в:
- `EXPENSE_CATEGORIES_COMPLETE.md`
- `docs/EXPENSE_CATEGORIES_GUIDE.md`
