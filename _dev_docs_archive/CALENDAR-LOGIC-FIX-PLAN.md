# 📅 ПЛАН ИСПРАВЛЕНИЯ ЛОГИКИ КАЛЕНДАРЯ

**Проблема:** Счетчики не обновляются по дням, выполненные задачи переносятся вместо сохранения в своих днях.

## 🔍 АНАЛИЗ ТЕКУЩИХ ПРОБЛЕМ

### **1. getMaterializedTasks() - Неправильный диапазон**
```typescript
// ПРОБЛЕМА: Диапазон -7 до +1 дня
const startDate = new Date(baseDate);
startDate.setDate(startDate.getDate() - 7); // ← НЕПРАВИЛЬНО
const endDate = new Date(baseDate);
endDate.setDate(endDate.getDate() + 1);   // ← НЕПРАВИЛЬНО
```

### **2. Логика статусов задач**
- Выполненные задачи показываются во всех днях диапазона
- Просроченные задачи не накапливаются правильно
- Счетчики остаются статичными

## 🎯 ПРАВИЛЬНАЯ ЛОГИКА

### **Сегодня (29.10.2025):**
- ✅ **Текущие**: задачи на сегодня (новые каждый день)
- ❌ **Просроченные**: 0 (сегодня ничего не может быть просрочено)
- ✅ **Выполненные**: только те, что выполнили именно сегодня

### **Завтра (30.10.2025):**
- ✅ **Текущие**: задачи на завтра (новые)
- ❌ **Просроченные**: невыполненные ежедневные с 29.10
- ✅ **Выполненные**: только те, что выполнят 30.10

### **Предыдущие дни (28.10.2025):**
- ✅ **Текущие**: 0 (день прошел)
- ❌ **Просроченные**: 0 (уже учтены в следующих днях)
- ✅ **Выполненные**: только те, что выполнили именно 28.10

## 🔧 ПЛАН ИСПРАВЛЕНИЙ

### **ЭТАП 1: Исправить getMaterializedTasks**
```typescript
// ПРАВИЛЬНО: Только задачи конкретного дня
const startDate = startOfDay(baseDate);
const endDate = endOfDay(baseDate);

whereClause.completedAt = {
  gte: startDate,
  lte: endDate
};
```

### **ЭТАП 2: Исправить логику просроченных**
```typescript
// Просроченные = ежедневные задачи с предыдущих дней, которые не выполнены
const getOverdueTasks = (currentDate) => {
  // Ищем невыполненные ежедневные задачи с дат < currentDate
  // Только для ежедневных задач!
}
```

### **ЭТАП 3: Исправить группировку по статусам**
```typescript
export function groupTasksByStatus(tasks: UnifiedTask[], baseDate: Date) {
  const today = startOfDay(baseDate);
  
  return {
    // Только задачи на конкретную дату
    today: tasks.filter(task => 
      task.status === 'AVAILABLE' && 
      isSameDay(task.scheduledDate, baseDate)
    ),
    
    // Просроченные ежедневные с предыдущих дней
    overdue: tasks.filter(task => 
      task.status === 'OVERDUE' && 
      task.scheduledDate < today &&
      isDailyTask(task)
    ),
    
    // Выполненные именно в этот день
    completed: tasks.filter(task => 
      task.status === 'COMPLETED' && 
      task.completedAt &&
      isSameDay(task.completedAt, baseDate)
    )
  };
}
```

### **ЭТАП 4: Добавить функцию сброса счетчиков**
```typescript
// Безопасный сброс без повреждения БД
export async function resetTaskCounters() {
  // НЕ удаляем данные, только пересчитываем статусы
  console.log('🔄 Пересчет счетчиков задач...');
}
```

## ⚠️ МЕРЫ БЕЗОПАСНОСТИ

### **Перед изменениями:**
1. ✅ Создать бэкап базы данных
2. ✅ Зафиксировать текущее состояние в git
3. ✅ Протестировать на копии данных

### **Во время изменений:**
1. ✅ НЕ удалять существующие записи в БД
2. ✅ Только изменять логику фильтрации
3. ✅ Сохранить все выполненные задачи

### **После изменений:**
1. ✅ Проверить корректность счетчиков
2. ✅ Убедиться, что история сохранена
3. ✅ Протестировать переходы между днями

## 🧪 ПЛАН ТЕСТИРОВАНИЯ

### **Тест 1: Сегодняшний день**
- Открыть календарь на сегодня
- Проверить: текущие > 0, просроченные = 0, выполненные = 0

### **Тест 2: Выполнение задачи**
- Выполнить задачу сегодня
- Проверить: выполненные +1, текущие -1

### **Тест 3: Переход на завтра**
- Открыть календарь на завтра
- Проверить: просроченные = невыполненные ежедневные с сегодня

### **Тест 4: Возврат на вчера**
- Открыть календарь на вчера
- Проверить: выполненные = только те, что выполнили вчера

---
**ГОТОВ К БЕЗОПАСНОМУ ИСПРАВЛЕНИЮ ЛОГИКИ КАЛЕНДАРЯ**
