# 📋 Новая логика системы статей расходов

## 🎯 Требования

### 1. Главная страница `/inventory`
- ✅ Показывать общие лимиты по объектам (как сейчас)
- ✅ Без дополнительных вкладок

### 2. Кнопка "Лимит"
- Открывает модальное окно
- Выбор статьи расходов из списка
- Ввод суммы лимита
- **Для админа:** кнопка "Создать новую статью" прямо в модальном окне
- Сохранение лимита по выбранной статье

### 3. Кнопка "Расход"
- Открывает модальное окно
- Выбор статьи расходов из списка
- Ввод суммы и описания
- Сохранение расхода по выбранной статье

### 4. Кнопка "Подробно"
- Открывает модальное окно с **2 вкладками**:
  - **"Общее"** - текущая аналитика (общие цифры)
  - **"По статьям"** - новая вкладка с разбивкой по статьям

## 📦 Что нужно сделать

### Шаг 1: Обновить модальное окно "Лимит" ✅
Файл: `src/components/InventoryFinancialReport.tsx` → `EditLimitModal`

**Добавить:**
- Выпадающий список категорий
- Для админа: кнопка "+ Создать категорию"
- Inline форма создания категории (показывается при клике)
- Сохранение лимита с привязкой к категории

### Шаг 2: Обновить модальное окно "Расход" ✅
Файл: `src/components/InventoryFinancialReport.tsx` → `AddExpenseModal`

**Добавить:**
- Выпадающий список категорий
- Опция "Без категории"
- Сохранение расхода с привязкой к категории

### Шаг 3: Обновить модальное окно "Подробно" ✅
Файл: `src/components/InventoryFinancialReport.tsx` → `ExpenseChartModal`

**Добавить:**
- Вкладки: "Общее" и "По статьям"
- Вкладка "Общее": текущий контент (график, общие цифры)
- Вкладка "По статьям": 
  - Карточки по каждой категории
  - Потрачено / Лимит / Остаток
  - Прогресс-бары
  - Процент использования

### Шаг 4: Seed данных ✅
- Создать 10 предустановленных категорий при первом запуске

## 🎨 Дизайн модальных окон

### Модальное окно "Лимит"

```
┌─────────────────────────────────────────┐
│ Редактировать лимит - Офис А            │
├─────────────────────────────────────────┤
│                                          │
│ Категория расходов: *                   │
│ [Выберите категорию ▼]                  │
│                                          │
│ [+ Создать новую категорию] (для админа)│
│                                          │
│ ┌─────────────────────────────────────┐ │
│ │ Новая категория:                    │ │
│ │ [Название_________________]         │ │
│ │ [Описание_________________]         │ │
│ │ [Создать] [Отмена]                  │ │
│ └─────────────────────────────────────┘ │
│                                          │
│ Сумма лимита (₽): *                     │
│ [10000_____]                            │
│                                          │
│ Месяц: [Ноябрь ▼]  Год: [2025 ▼]       │
│                                          │
│ [✓ Повторяющийся лимит]                 │
│                                          │
│ [Сохранить] [Отмена]                    │
└─────────────────────────────────────────┘
```

### Модальное окно "Расход"

```
┌─────────────────────────────────────────┐
│ Добавить расход - Офис А                │
├─────────────────────────────────────────┤
│                                          │
│ Категория расходов:                     │
│ [Выберите категорию ▼]                  │
│ (или оставьте пустым)                   │
│                                          │
│ Сумма (₽): *                            │
│ [500_______]                            │
│                                          │
│ Описание: *                             │
│ [Моющее средство__________]             │
│ [_________________________]             │
│                                          │
│ ⚠️ Использовано 85% лимита по категории │
│                                          │
│ [Добавить] [Отмена]                     │
└─────────────────────────────────────────┘
```

### Модальное окно "Подробно"

```
┌──────────────────────────────────────────────────┐
│ Детальная информация - Офис А                    │
├──────────────────────────────────────────────────┤
│ [Общее] [По статьям]                             │
├──────────────────────────────────────────────────┤
│                                                   │
│ Вкладка "Общее":                                 │
│ - График расходов (текущий)                      │
│ - Общие цифры                                    │
│                                                   │
│ Вкладка "По статьям":                            │
│ ┌────────────────────────────────────────────┐   │
│ │ 🧪 Химия, инвентарь, расходники            │   │
│ │ Потрачено: 15,000₽  Лимит: 30,000₽        │   │
│ │ Остаток: 15,000₽                           │   │
│ │ [████████████░░░░░░░░] 50%                 │   │
│ └────────────────────────────────────────────┘   │
│                                                   │
│ ┌────────────────────────────────────────────┐   │
│ │ 👕 Спецодежда                              │   │
│ │ Потрачено: 4,500₽  Лимит: 5,000₽          │   │
│ │ Остаток: 500₽                              │   │
│ │ [█████████████████░░] 90% ⚠️               │   │
│ └────────────────────────────────────────────┘   │
│                                                   │
│ [Закрыть]                                        │
└──────────────────────────────────────────────────┘
```

## 🔄 Поток данных

### Создание лимита с категорией:
```
1. Админ открывает модальное окно "Лимит"
2. Выбирает категорию из списка (или создает новую)
3. Вводит сумму лимита
4. Сохраняет
5. API: POST /api/objects/:id/expense-limits
   {
     categoryId: "cat_id",
     amount: 10000,
     month: 11,
     year: 2025
   }
```

### Добавление расхода с категорией:
```
1. Менеджер открывает модальное окно "Расход"
2. Выбирает категорию (опционально)
3. Вводит сумму и описание
4. Сохраняет
5. API: POST /api/objects/:id/expenses
   {
     categoryId: "cat_id" или null,
     amount: 500,
     description: "Моющее средство"
   }
6. Система автоматически проверяет лимиты
7. Если лимит превышен → ошибка
8. Если >90% → предупреждение
```

### Просмотр статистики по категориям:
```
1. Пользователь открывает модальное окно "Подробно"
2. Переключается на вкладку "По статьям"
3. API: GET /api/expenses/stats/:objectId?month=11&year=2025
4. Отображаются карточки по каждой категории
```

## ✅ Чеклист реализации

- [ ] Обновить `EditLimitModal` - добавить выбор категории
- [ ] Добавить inline создание категории для админа
- [ ] Обновить `AddExpenseModal` - добавить выбор категории
- [ ] Обновить `ExpenseChartModal` - добавить вкладку "По статьям"
- [ ] Создать компонент карточки категории для статистики
- [ ] Применить миграцию БД
- [ ] Создать seed данных для категорий
- [ ] Протестировать весь флоу

## 🎉 Результат

**Пользователь получает:**
- ✅ Простой интерфейс без лишних вкладок
- ✅ Быстрое создание лимитов с категориями
- ✅ Удобное добавление расходов с категориями
- ✅ Детальную аналитику по категориям в одном месте
- ✅ Возможность создавать новые категории на лету (админ)

**Админ может:**
- Создавать категории прямо при создании лимита
- Видеть детальную разбивку по категориям
- Контролировать лимиты по каждой категории

**Менеджер может:**
- Выбирать категорию при добавлении расхода
- Видеть предупреждения о превышении лимита
- Просматривать статистику по категориям

---

**Следующий шаг:** Реализовать обновления модальных окон
