# 🚀 НАЧНИТЕ ОТСЮДА - Создание презентационной копии

## ✅ ВСЁ ГОТОВО! Следуйте этим шагам:

---

## 📋 ШАГ 1: СОЗДАЙТЕ КОПИЮ ПРОЕКТА

Откройте PowerShell и выполните:

```powershell
# Перейдите в папку Documents
cd C:\Users\Тайм\Documents\

# Создайте копию проекта (займет 1-2 минуты)
xcopy cleaning_control cleaning_control-presentation /E /I /H
```

**Дождитесь завершения!** Должна появиться папка `cleaning_control-presentation`

---

## 🧹 ШАГ 2: ОЧИСТИТЕ КОПИЮ

```powershell
# Перейдите в копию
cd cleaning_control-presentation

# Удалите node_modules
Remove-Item -Recurse -Force node_modules

# Удалите старые .env файлы
Remove-Item .env -ErrorAction SilentlyContinue
Remove-Item .env.local -ErrorAction SilentlyContinue

# Удалите кеш
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
```

---

## 📄 ШАГ 3: СКОПИРУЙТЕ ГОТОВЫЙ .ENV.LOCAL

```powershell
# Скопируйте готовый файл из рабочего проекта
copy ..\cleaning_control\env.local.READY .env.local
```

**ИЛИ** создайте файл `.env.local` вручную и вставьте содержимое из `env.local.READY`

---

## 📦 ШАГ 4: УСТАНОВИТЕ ЗАВИСИМОСТИ

```powershell
# Убедитесь что вы в папке cleaning_control-presentation
cd C:\Users\Тайм\Documents\cleaning_control-presentation

# Установите зависимости
npm install

# Установите faker для тестовых данных
npm install @faker-js/faker --save-dev
```

**Дождитесь завершения** (2-3 минуты)

---

## 🗄️ ШАГ 5: СОЗДАЙТЕ СТРУКТУРУ БД В SUPABASE

```powershell
# Сгенерируйте Prisma Client
npx prisma generate

# Создайте все таблицы в Supabase
npx prisma db push
```

**Должно появиться:** "Your database is now in sync with your Prisma schema"

---

## 🧪 ШАГ 6: ЗАПОЛНИТЕ ТЕСТОВЫМИ ДАННЫМИ

```powershell
# Запустите скрипт генерации данных
npx tsx scripts/seed-presentation.ts
```

**Дождитесь завершения** (30-60 секунд)

Должна появиться статистика созданных данных.

---

## 📦 ШАГ 7: НАСТРОЙТЕ SUPABASE STORAGE (ОПЦИОНАЛЬНО)

**Для загрузки фото в презентации:**

1. Откройте [app.supabase.com](https://app.supabase.com)
2. Storage → New bucket → Name: `uploads` → Public: ✅
3. В `.env.local` раскомментируйте:
   ```env
   NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET="uploads"
   ```

**Подробная инструкция:** `SUPABASE_STORAGE_SETUP.md`

**Можно пропустить** - фото будут работать и без этого (но не будут сохраняться).

---

## 🚀 ШАГ 8: ЗАПУСТИТЕ ПРЕЗЕНТАЦИОННУЮ КОПИЮ

```powershell
# Запустите на порту 3001 (чтобы не конфликтовать с рабочим проектом)
npm run dev -- -p 3001
```

**Откройте браузер:** http://localhost:3001

---

## 🔑 ШАГ 9: ВОЙДИТЕ В СИСТЕМУ

```
Email: admin@example.com
Пароль: password123
```

**Другие учетные записи:**
- Заместитель: deputy@example.com / password123
- Бухгалтер: accountant@example.com / password123
- Менеджеры: manager1@example.com (до manager4) / password123

---

## ✅ ГОТОВО! 🎉

Теперь у вас есть:
- ✅ Рабочий проект: `cleaning_control` (Timeweb БД)
- ✅ Презентационная копия: `cleaning_control-presentation` (Supabase БД)

---

## 💻 КАК РАБОТАТЬ С ДВУМЯ ПРОЕКТАМИ:

### Вариант 1: Запускать по очереди

**Рабочий проект:**
```powershell
cd C:\Users\Тайм\Documents\cleaning_control
npm run dev
# → http://localhost:3000
```

**Презентация:**
```powershell
cd C:\Users\Тайм\Documents\cleaning_control-presentation
npm run dev -- -p 3001
# → http://localhost:3001
```

### Вариант 2: Запустить оба одновременно

Откройте **два терминала PowerShell**:

**Терминал 1 - Рабочий:**
```powershell
cd C:\Users\Тайм\Documents\cleaning_control
npm run dev
```

**Терминал 2 - Презентация:**
```powershell
cd C:\Users\Тайм\Documents\cleaning_control-presentation
npm run dev -- -p 3001
```

### Вариант 3: Два окна VS Code

1. **Текущее окно:** `cleaning_control`
2. **File → New Window**
3. **File → Open Folder** → выберите `cleaning_control-presentation`

---

## 📤 ДЕПЛОЙ НА VERCEL (когда будете готовы)

```powershell
cd C:\Users\Тайм\Documents\cleaning_control-presentation

# Инициализируйте git (если нужно)
git init
git add .
git commit -m "Presentation copy"

# Установите Vercel CLI (если еще не установлен)
npm install -g vercel

# Войдите и разверните
vercel login
vercel
```

**Не забудьте:**
1. Добавить переменные окружения в Vercel Dashboard
2. Обновить URL в Supabase (Authentication → URL Configuration)

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК:

### Ошибка при копировании проекта
```powershell
# Убедитесь что вы в правильной папке
cd C:\Users\Тайм\Documents\
# Попробуйте снова
xcopy cleaning_control cleaning_control-presentation /E /I /H
```

### Ошибка "Can't reach database server"
- Проверьте что DATABASE_URL правильный в `.env.local`
- Проверьте что проект Supabase активен

### Ошибка при prisma db push
```powershell
# Попробуйте сначала:
npx prisma generate
# Потом снова:
npx prisma db push
```

### Не могу войти в систему
- Проверьте что скрипт `seed-presentation.ts` выполнился успешно
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь что JWT_SECRET и NEXTAUTH_SECRET одинаковые

---

## 📊 ИТОГОВАЯ СТРУКТУРА:

```
C:\Users\Тайм\Documents\
│
├── cleaning_control\                  ← РАБОЧИЙ ПРОЕКТ
│   ├── .env                           ← Timeweb БД
│   ├── .env.local                     ← Timeweb БД
│   ├── node_modules\
│   └── ...
│
└── cleaning_control-presentation\     ← ПРЕЗЕНТАЦИЯ
    ├── .env.local                     ← Supabase БД (НОВАЯ)
    ├── node_modules\
    └── ...
```

---

## 🎯 ВАЖНЫЕ ФАЙЛЫ:

- **`env.local.READY`** - Готовый .env.local для презентационной копии
- **`CREATE_SEPARATE_COPY.md`** - Подробная инструкция
- **`QUICK_START_SEPARATE_COPY.md`** - Быстрая памятка
- **`scripts/seed-presentation.ts`** - Скрипт генерации тестовых данных

---

## 🎉 УДАЧИ С ПРЕЗЕНТАЦИЕЙ!

Если возникнут вопросы - обращайтесь! 😊
