# 📋 ДОКУМЕНТАЦИЯ: ВСЕ СЦЕНАРИИ ИЕРАРХИИ

## 🎯 Обзор

Система поддерживает **10 различных сценариев** иерархической структуры объектов уборки. Каждый сценарий правильно обрабатывается при загрузке Excel, создавая необходимые виртуальные уровни для соблюдения ограничений БД.

---

## ✅ ПРОТЕСТИРОВАННЫЕ СЦЕНАРИИ

### 1️⃣ **Полная иерархия**
```
Excel: Участок → Зона → Группа → Помещение → Объект уборки → Техкарта
БД:    Участок → Зона → Группа → Помещение → Объект уборки → Техкарта
UI:    Участок → Зона → Группа → Помещение → Объект уборки → Техкарта
```
**Пример:** Административное здание → 1 этаж → Офисы → Кабинет 101 → Окна → Мойка окон

---

### 2️⃣ **Без участка**
```
Excel: [пусто] → Зона → Группа → Помещение → Объект уборки → Техкарта
БД:    __VIRTUAL__ → Зона → Группа → Помещение → Объект уборки → Техкарта
UI:    Зона → Группа → Помещение → Объект уборки → Техкарта
```
**Пример:** 1 этаж → Офисы → Кабинет 101 → Окна → Мойка окон

---

### 3️⃣ **Без зоны**
```
Excel: Участок → [пусто] → Группа → Помещение → Объект уборки → Техкарта
БД:    Участок → __VIRTUAL__ → Группа → Помещение → Объект уборки → Техкарта
UI:    Участок → Группа → Помещение → Объект уборки → Техкарта
```
**Пример:** Административное здание → Офисы → Кабинет 101 → Окна → Мойка окон

---

### 4️⃣ **Без группы помещений** ⚠️ ИСПРАВЛЕНО
```
Excel: Участок → Зона → [пусто] → Помещение → Объект уборки → Техкарта
БД:    Участок → Зона → __VIRTUAL__ → Помещение → Объект уборки → Техкарта
UI:    Участок → Зона → Помещение → Объект уборки → Техкарта
```
**Пример:** Административное здание → 1 этаж → Кабинет 101 → Окна → Мойка окон

---

### 5️⃣ **Без помещения**
```
Excel: Участок → Зона → Группа → [пусто] → Объект уборки → Техкарта
БД:    Участок → Зона → Группа → __VIRTUAL__ → Объект уборки → Техкарта
UI:    Участок → Зона → Группа → Объект уборки → Техкарта
```
**Пример:** Административное здание → 1 этаж → Офисы → Окна → Мойка окон

---

### 6️⃣ **Без объекта уборки**
```
Excel: Участок → Зона → Группа → Помещение → [пусто] → Техкарта
БД:    Участок → Зона → Группа → Помещение → Техкарта
UI:    Участок → Зона → Группа → Помещение → Техкарта
```
**Пример:** Административное здание → 1 этаж → Офисы → Кабинет 101 → Влажная уборка

---

### 7️⃣ **Только группа и техкарта**
```
Excel: [пусто] → [пусто] → Группа → [пусто] → [пусто] → Техкарта
БД:    __VIRTUAL__ → __VIRTUAL__ → Группа → __VIRTUAL__ → Техкарта
UI:    Группа → Техкарта
```
**Пример:** Офисы → Влажная уборка

---

### 8️⃣ **Только зона и техкарта** ⚠️ ИСПРАВЛЕНО (ПРОБЛЕМНЫЙ СЦЕНАРИЙ)
```
Excel: [пусто] → Зона → [пусто] → [пусто] → [пусто] → Техкарта
БД:    __VIRTUAL__ → Зона → __VIRTUAL__ → __VIRTUAL__ → Техкарта
UI:    Зона → Техкарта
```
**Пример:** 2 этаж → Протирка стеллажей

**БЫЛО:** Зона создавалась пустой, техкарта не привязывалась  
**СТАЛО:** Создаются виртуальные группа и помещение, техкарта корректно привязывается

---

### 9️⃣ **Только участок и техкарта**
```
Excel: Участок → [пусто] → [пусто] → [пусто] → [пусто] → Техкарта
БД:    Участок → __VIRTUAL__ → __VIRTUAL__ → __VIRTUAL__ → Техкарта
UI:    Участок → Техкарта
```
**Пример:** Административное здание → Генеральная уборка

---

### 🔟 **Только техкарта**
```
Excel: [пусто] → [пусто] → [пусто] → [пусто] → [пусто] → Техкарта
БД:    Техкарта (roomId: null, cleaningObjectItemId: null)
UI:    Техкарта (на верхнем уровне объекта)
```
**Пример:** Дополнительные услуги

---

## 🔧 ЛОГИКА СОЗДАНИЯ ВИРТУАЛЬНЫХ УРОВНЕЙ

### Правила:

1. **Участок (`Site`):**
   - Если есть → создается реальный
   - Если нет, но есть зона/группа/помещение/объект уборки → создается `__VIRTUAL__`

2. **Зона (`Zone`):**
   - Если есть → создается реальная
   - Если нет, но есть группа/помещение/объект уборки → создается `__VIRTUAL__`
   - **Требует:** `siteId` (реальный или виртуальный)

3. **Группа помещений (`RoomGroup`):**
   - Если есть → создается реальная
   - Если нет, но есть зона → **ВСЕГДА** создается `__VIRTUAL__` ⚠️ ИСПРАВЛЕНО
   - **Требует:** `zoneId` (реальный или виртуальный)

4. **Помещение (`Room`):**
   - Если есть → создается реальное
   - Если нет, но есть группа → создается `__VIRTUAL__`
   - **Требует:** `roomGroupId` (реальный или виртуальный)

5. **Объект уборки (`CleaningObjectItem`):**
   - Если есть → создается реальный
   - Если нет → техкарта привязывается напрямую к помещению
   - **Требует:** `roomId` (реальный или виртуальный)

6. **Техкарта (`TechCard`):**
   - Всегда создается
   - Привязывается к максимально доступному уровню:
     - `cleaningObjectItemId` (если есть объект уборки)
     - `roomId` (если есть помещение)
     - `objectId` (если ничего нет)

---

## 🎨 ОТОБРАЖЕНИЕ В UI

### Правила фильтрации:

- **Виртуальные участки** (`Site.name === '__VIRTUAL__'`) → **СКРЫТЫ**, показываются их зоны напрямую
- **Виртуальные зоны** (`Zone.name === '__VIRTUAL__'`) → **СКРЫТЫ**, показываются их группы напрямую
- **Виртуальные группы** (`RoomGroup.name === '__VIRTUAL__'`) → **СКРЫТЫ**, показываются их помещения напрямую
- **Виртуальные помещения** (`Room.name === '__VIRTUAL__'`) → **СКРЫТЫ**, показываются их объекты уборки/техкарты напрямую

### Примеры отображения:

**Сценарий: Участок + Техкарта**
```
БД:  Участок → __VIRTUAL__ → __VIRTUAL__ → __VIRTUAL__ → Техкарта
            (зона)        (группа)      (помещение)

UI:  Участок
       └─ Техкарта 1
       └─ Техкарта 2
       └─ Техкарта 3
```

**Сценарий: Зона + Техкарта**
```
БД:  __VIRTUAL__ → Зона → __VIRTUAL__ → __VIRTUAL__ → Техкарта
     (участок)           (группа)      (помещение)

UI:  Зона
       └─ Техкарта 1
       └─ Техкарта 2
```

### Результат:
Пользователь видит **только реальные уровни** из Excel, виртуальные уровни полностью прозрачны.

---

## 🧪 ТЕСТИРОВАНИЕ

### Полный тест всех сценариев:

```powershell
npx tsx scripts/test-all-hierarchy-scenarios.ts
```

**Ожидаемый результат:**
```
✅ Успешно: 10/10
❌ Ошибок: 0/10
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ!
```

### Тест сценария "Участок + Техкарта":

```powershell
# Создать тестовые данные
npx tsx scripts/test-site-only-scenario.ts

# Проверить в UI что виртуальные уровни скрыты

# Удалить тестовые данные
npx tsx scripts/cleanup-test-site.ts
```

---

## 📝 ИСТОРИЯ ИЗМЕНЕНИЙ

### v1.1 (09.11.2025 - 14:00)
- ✅ Исправлен сценарий #9 (участок + техкарта)
- ✅ Добавлена функция `processSite()` для фильтрации виртуальных участков в UI
- ✅ Виртуальные участки теперь корректно скрываются
- ✅ Создан тест `test-site-only-scenario.ts` для проверки

### v1.0 (09.11.2025 - 13:00)
- ✅ Реализована поддержка всех 10 сценариев
- ✅ Исправлен сценарий #8 (зона + техкарта)
- ✅ Исправлен сценарий #4 (без группы помещений)
- ✅ Добавлена автоматическая фильтрация виртуальных уровней в UI
- ✅ Все тесты пройдены успешно

---

## 🚀 ИСПОЛЬЗОВАНИЕ

При загрузке Excel система автоматически:
1. Определяет, какие уровни заполнены
2. Создает виртуальные уровни для пропущенных обязательных полей
3. Привязывает техкарты к правильному уровню
4. Фильтрует виртуальные уровни в UI

**Никаких дополнительных действий не требуется!** 🎉
