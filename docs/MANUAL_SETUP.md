# 📚 НАСТРОЙКА ВСТРОЕННОГО МАНУАЛА

## Что было создано

Встроенный мануал с автоматической загрузкой скриншотов и удобной навигацией.

---

## 📁 Структура файлов

### 1. База данных (Prisma)

**Файл:** `prisma/schema.prisma`

Добавлены две модели:
- `ManualSection` - разделы мануала
- `ManualScreenshot` - метаданные скриншотов

### 2. API маршруты

**Созданные файлы:**
- `src/app/api/manual/sections/route.ts` - список всех разделов
- `src/app/api/manual/sections/[slug]/route.ts` - контент конкретного раздела
- `src/app/api/manual/screenshots/route.ts` - список скриншотов

### 3. Компоненты

**Созданные файлы:**
- `src/components/ManualViewer.tsx` - главный компонент просмотра мануала
- `src/components/ui/scroll-area.tsx` - компонент прокрутки

**Изменённые файлы:**
- `src/app/login/page.tsx` - добавлена кнопка "Инструкция пользователя"
- `src/components/AppLayout.tsx` - добавлен пункт меню "Инструкция"

### 4. Скрипты

**Файл:** `scripts/seed-manual.ts`

Скрипт для заполнения базы данных контентом из markdown файлов.

---

## 🚀 ИНСТРУКЦИЯ ПО УСТАНОВКЕ

### Шаг 1: Установить зависимости

```bash
npm install react-markdown
```

### Шаг 2: Применить миграцию Prisma

```bash
npx prisma migrate dev --name add_manual_tables
```

Это создаст таблицы `manual_sections` и `manual_screenshots` в базе данных.

### Шаг 3: Сгенерировать Prisma Client

```bash
npx prisma generate
```

### Шаг 4: Заполнить базу данных контентом

```bash
npx tsx scripts/seed-manual.ts
```

Этот скрипт:
- Прочитает все markdown файлы из `docs/`
- Найдёт все метки скриншотов `📸 [СКРИНШОТ: ...]`
- Заменит их на `{{SCREENSHOT:N}}`
- Создаст записи в базе данных
- Присвоит каждому скриншоту уникальный номер

**Вывод скрипта покажет:**
- Сколько разделов создано
- Сколько скриншотов найдено
- Номера и описания всех скриншотов

### Шаг 5: Создать папку для скриншотов

```bash
mkdir public\manual
mkdir public\manual\screenshots
```

### Шаг 6: Добавить скриншоты

После выполнения скрипта вы увидите список всех скриншотов с номерами:

```
📸 Скриншот 1: Страница входа в систему
📸 Скриншот 2: Главная страница с боковым меню
📸 Скриншот 3: Список объектов с карточками
...
```

**Сделайте скриншоты и сохраните их с соответствующими именами:**

```
public/manual/screenshots/screenshot-001.png  ← Страница входа
public/manual/screenshots/screenshot-002.png  ← Главная страница
public/manual/screenshots/screenshot-003.png  ← Список объектов
...
```

**Формат имени:** `screenshot-XXX.png` где XXX - трёхзначный номер (001, 002, 003...)

---

## 📸 КАК ДОБАВИТЬ СКРИНШОТЫ

### Вариант 1: Вручную

1. Запустите приложение
2. Откройте каждую страницу
3. Сделайте скриншот (Windows: Win+Shift+S)
4. Сохраните как `screenshot-001.png`, `screenshot-002.png` и т.д.
5. Поместите в `public/manual/screenshots/`

### Вариант 2: Автоматически (рекомендуется)

Создайте скрипт для автоматического создания скриншотов с помощью Playwright:

```bash
npm install -D @playwright/test
```

Создайте файл `scripts/take-screenshots.ts`:

```typescript
import { chromium } from '@playwright/test';

async function takeScreenshots() {
  const browser = await chromium.launch();
  const page = await browser.newPage();
  
  // Войдите в систему
  await page.goto('http://localhost:3000/login');
  await page.fill('input[type="email"]', 'admin@example.com');
  await page.fill('input[type="password"]', 'password');
  await page.click('button[type="submit"]');
  
  // Список страниц для скриншотов
  const screenshots = [
    { url: '/login', name: 'screenshot-001.png' },
    { url: '/', name: 'screenshot-002.png' },
    { url: '/objects', name: 'screenshot-003.png' },
    // ... добавьте все нужные страницы
  ];
  
  for (const { url, name } of screenshots) {
    await page.goto(`http://localhost:3000${url}`);
    await page.waitForTimeout(1000);
    await page.screenshot({ 
      path: `public/manual/screenshots/${name}`,
      fullPage: true 
    });
    console.log(`✅ ${name}`);
  }
  
  await browser.close();
}

takeScreenshots();
```

Запустите:

```bash
npx tsx scripts/take-screenshots.ts
```

---

## 🎨 КАК РАБОТАЕТ МАНУАЛ

### Доступ к мануалу

**1. До входа в систему:**
- Кнопка "Инструкция пользователя" на странице входа

**2. После входа:**
- Пункт меню "Инструкция" (после Telegram)

### Интерфейс мануала

**Левое меню:**
- Список всех разделов с иконками
- Клик по разделу загружает его контент

**Основная область:**
- Отформатированный контент раздела
- Автоматически вставленные скриншоты
- Кнопки навигации (Назад/Вперёд)

**Скриншоты:**
- Автоматически загружаются из `public/manual/screenshots/`
- Клик по скриншоту открывает увеличенное изображение
- Zoom: 100%, увеличить, уменьшить

### Если скриншот не найден

Вместо изображения показывается placeholder с описанием и именем файла.

---

## 📝 РЕДАКТИРОВАНИЕ КОНТЕНТА

### Где находятся тексты мануала?

**В базе данных!**

После выполнения `seed-manual.ts` весь контент находится в таблице `manual_sections`.

### Как изменить текст?

**Вариант 1: Через базу данных**

```sql
UPDATE manual_sections 
SET content = 'Новый текст...' 
WHERE slug = '01-nachalo-raboty';
```

**Вариант 2: Изменить markdown и перезапустить seed**

1. Отредактируйте файл `docs/01_НАЧАЛО_РАБОТЫ.md`
2. Запустите `npx tsx scripts/seed-manual.ts`
3. Контент обновится в базе

**Вариант 3: Создать админ-панель (будущее)**

Можно создать страницу `/admin/manual` для редактирования разделов через веб-интерфейс.

### Как добавить новый раздел?

1. Создайте markdown файл в `docs/`
2. Добавьте раздел в массив `manualSections` в `scripts/seed-manual.ts`:

```typescript
{
  slug: '08-novyy-razdel',
  title: 'Новый раздел',
  icon: '🆕',
  order: 8,
  filename: '08_НОВЫЙ_РАЗДЕЛ.md'
}
```

3. Запустите `npx tsx scripts/seed-manual.ts`

---

## 🔧 ТЕХНИЧЕСКАЯ ИНФОРМАЦИЯ

### Формат меток скриншотов

В markdown файлах используется формат:

```markdown
📸 [СКРИНШОТ: Описание скриншота]
```

Скрипт автоматически:
1. Находит все такие метки
2. Присваивает им номера (1, 2, 3...)
3. Заменяет на `{{SCREENSHOT:N}}`
4. Создаёт записи в `manual_screenshots`

### Как отображаются скриншоты

Компонент `ManualViewer` ищет в контенте метки `{{SCREENSHOT:N}}` и заменяет их на:

```tsx
<img src="/manual/screenshots/screenshot-001.png" alt="Описание" />
```

### Путь к скриншотам

```
public/manual/screenshots/screenshot-XXX.png
```

Доступны по URL: `/manual/screenshots/screenshot-XXX.png`

---

## 📊 СТАТИСТИКА

После выполнения `seed-manual.ts` вы увидите:

```
✅ Заполнение завершено!
📊 Создано разделов: 7
📸 Всего скриншотов: 200+

📁 Поместите скриншоты в папку: public/manual/screenshots/
   Формат имени: screenshot-001.png, screenshot-002.png, и т.д.
```

---

## ❓ ЧАСТЫЕ ВОПРОСЫ

**Q: Можно ли использовать другие форматы изображений?**  
A: Да, измените расширение в `seed-manual.ts` (строка `filename: screenshot-${...}.png`)

**Q: Как изменить порядок разделов?**  
A: Измените поле `order` в базе данных или в `manualSections` массиве

**Q: Можно ли добавить видео?**  
A: Да, аналогично скриншотам создайте метки `{{VIDEO:N}}` и обработайте их в `ManualViewer`

**Q: Как сделать мануал доступным только для определённых ролей?**  
A: Добавьте проверку роли в `ManualViewer` или в API маршрутах

**Q: Можно ли экспортировать мануал в PDF?**  
A: Да, добавьте библиотеку для генерации PDF (например, `jspdf` или `puppeteer`)

---

## 🎯 ИТОГОВЫЙ ЧЕКЛИСТ

- [ ] Установить `react-markdown`
- [ ] Применить миграцию Prisma
- [ ] Сгенерировать Prisma Client
- [ ] Запустить `seed-manual.ts`
- [ ] Создать папку `public/manual/screenshots/`
- [ ] Сделать и добавить все скриншоты
- [ ] Проверить работу мануала на странице входа
- [ ] Проверить работу мануала в главном меню
- [ ] Проверить все разделы
- [ ] Проверить увеличение скриншотов

---

## 📞 ПОДДЕРЖКА

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи сервера
3. Убедитесь что миграция применена
4. Убедитесь что скрипт seed выполнен успешно
5. Проверьте что скриншоты находятся в правильной папке

---

**Готово! Теперь у вас полноценный встроенный мануал с автоматической загрузкой скриншотов! 🎉**
