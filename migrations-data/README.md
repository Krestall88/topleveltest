# Система миграций данных

Эта папка содержит все скрипты миграции данных для отслеживания изменений в базе данных.

## Правила работы с миграциями

### 🚨 КРИТИЧЕСКИ ВАЖНО:
- **НИКОГДА НЕ ИСПОЛЬЗОВАТЬ `--force-reset`** без явного разрешения пользователя
- Всегда создавать файл миграции перед изменением схемы БД
- Документировать каждое изменение

### 📋 Процедура изменения БД:

1. **Создать файл миграции** в этой папке с именем: `YYYY-MM-DD_описание.js`
2. **Изменить schema.prisma** если нужно
3. **Выполнить** `npx prisma db push` (БЕЗ --force-reset)
4. **Применить миграцию данных** `node migrations-data/файл.js`
5. **Задокументировать** в CHANGELOG.md

### 📁 Структура файла миграции:

```javascript
// migrations-data/2025-10-12_add-inventory-system.js
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();

async function migrate() {
  console.log('🔄 Начало миграции: Добавление системы инвентаря');
  
  try {
    // Ваши изменения данных здесь
    
    console.log('✅ Миграция завершена успешно');
  } catch (error) {
    console.error('❌ Ошибка миграции:', error);
    throw error;
  } finally {
    await prisma.$disconnect();
  }
}

migrate();
```

### 📝 Логирование:
Каждая миграция должна логировать:
- Что изменяется
- Сколько записей затронуто
- Результат операции

### 🔄 Откат миграций:
Для критических изменений создавать файл отката: `YYYY-MM-DD_rollback-описание.js`
