# 🔧 НАСТРОЙКА SUPABASE - УПРОЩЕННЫЙ МЕТОД

## ✅ ИСПОЛЬЗУЕМ PRISMA ДЛЯ СОЗДАНИЯ СТРУКТУРЫ

Вместо ручного SQL-скрипта, мы используем Prisma для автоматического создания всей структуры БД.

### 📝 ЧТО ВЫ ДЕЛАЕТЕ:

1. **Получите строку подключения из Supabase** (из Шага 2 основной инструкции)

2. **Откройте файл `.env.local`** и добавьте/замените:
   ```
   DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxxxxxxxxx.supabase.co:5432/postgres"
   ```

3. **Откройте терминал** и выполните команды:

   ```bash
   # Генерируем Prisma Client
   npx prisma generate
   ```

   ```bash
   # Применяем схему к Supabase (создаем все таблицы, связи, индексы)
   npx prisma db push
   ```

4. **Дождитесь завершения** - должно появиться сообщение:
   ```
   Your database is now in sync with your Prisma schema.
   ```

**ВСЁ! Структура БД создана автоматически!** ✅

Prisma создал:
- ✅ Все таблицы
- ✅ Все связи (foreign keys)
- ✅ Все индексы
- ✅ Все enum типы
- ✅ Все ограничения (constraints)

---

## 🎯 СЛЕДУЮЩИЙ ШАГ

Переходите к **Шагу 6** основной инструкции - создание тестовых данных.
